# Техдолг и находки вне скоупа

Сюда попадает то, что ревью или исполнитель заметили в ходе задачи, но чинить в её рамках
нельзя (правило «не рефакторить заодно»). Одна запись - одна строка; чинится отдельной задачей
трекера, которую заводит оркестратор (`node scripts/tasks.mjs add`) или владелец.

Исходов у записи четыре: **открыто** (будет задача), **в работе** (id задачи), **закрыто**
(чем), **принято** (осознанно не чиним: цена и условие переоткрытия - оба обязательны).

| Дата | Откуда | Где | Что не так | Риск | Исход |
| --- | --- | --- | --- | --- | --- |
| 05.09.2026 | декомпозиция E0 | `.env.example`, `packages/config` | пустой `TOKEN_ENCRYPTION_KEY=` не проходит `length(64).optional()` (D2) | старт с примером конфига падает | закрыто: E0-03 (`normalizeValues` в `loadEnv`, тест на тексте `.env.example`) |
| 05.09.2026 | декомпозиция E0 | `packages/db/src/index.ts` | соединение с БД открывается при импорте (D3) | тесты и typecheck web зависят от БД | закрыто: E0-04 (`createDb(url)`/`closeDb(db)` в `src/client.ts`, `src/index.ts` - только реэкспорты; посадка `packages/db/test/no-side-effects.test.ts`) |
| 05.09.2026 | декомпозиция E0 | `packages/integrations/youtube/src/quota.ts` | тип `UsageLogger` лежит в интеграции, а нужен всем (D5) | дубли типа | закрыто: E0-05 (`ApiUsageEntry` и `UsageLogger` в `packages/core/src/domain/usage.ts`, реэкспорта из `@sf/youtube` нет; посадка - `packages/integrations/youtube/test/usage-logger.test.ts`) |
| 05.09.2026 | обвязка | `.claude/hooks` | нет стража боевой базы (`db-guard`) - боевой базы пока нет | при появлении VPS команды к проду не отличимы от локальных | открыто: завести до первого деплоя (E0-11/E13) |
| 06.09.2026 | E0-02, ревью | `packages/config/src/index.ts`, `packages/config/vitest/setup.ts` | два загрузчика env в одном пакете (`dotenv/config` в рантайме, `parseEnv`+`Object.assign` в тестовой фикстуре); после чистки ключей схемы `dotenv/config` дозаполняет удалённые ключи из `.env` в cwd, если такой файл есть | два разных контракта загрузки (необязательный/перекрывающий) держатся раздельно, легко разойтись дальше | закрыто: E0-03 (`src/env-file.ts` - один разбор `node:util.parseEnv`, `dotenv` удалён, чтение `.env` пропускается под `VITEST`) |
| 06.09.2026 | E0-02, ревью | `.env.test:MEDIA_DIR` | относительный путь `./data/media-test`; turbo запускает задачи из каталога пакета, `.gitignore` игнорирует только корневой `/data/` | артефакты тестов лягут в `apps/api/data/…`, `packages/core/data/…` и попадут в `git status` | закрыто: E0-03 (`MEDIA_DIR` в схеме резолвится от корня репозитория; значение фикстуры не менялось) |
| 06.09.2026 | E0-03, решение техлида | `.claude/hooks/rules.sf.json:106` (S11) | правило режет `from "@sf/config"` в `apps/web/src/**`, но не видит подвход `@sf/config/server`; клиентский импорт подвхода ловит только ручной `next build` (гейт `build` = `never`) | секрет в клиентском чанке попадёт в дифф без замка, пока сборка не в гейтах | открыто: включить гейт `build` (E0-01A) и дополнить проверку web - E0-10 |
| 06.09.2026 | E0-03, круг 2 | `packages/config/src/server.ts`, `apps/web/next.config.ts` | `server.ts` реэкспортирует `./index.js`, а webpack под `transpilePackages` не сопоставляет ESM-расширение `.js` исходнику `.ts`: без `extensionAlias` не резолвится ни ветка `default` корневого входа, ни ветка `browser` | серверный компилятор `next build` падает на `Module not found` раньше, чем стартует клиентский, поэтому маркер `server-only` в сборке не проявляется; условие `browser` доказывается только `node --conditions=browser` и юнит-тестом | закрыто: E0-03 (`experimental.extensionAlias: { ".js": [".ts", ".tsx", ".js"] }` в `apps/web/next.config.ts`; клиентский импорт `@sf/config/server` теперь валит сборку строкой про `server-only`) |
| 06.09.2026 | E0-03, круг 3 | `packages/config/src/paths.ts`, `apps/web/next.config.ts` | `repoRoot` собирается из `dirname(fileURLToPath(import.meta.url))` (круг 4), поэтому webpack больше не читает выражение как ссылку на ресурс и сборка проходит; но `import.meta.url` после бандла указывает внутрь `.next/server/**`, то есть корректность вычисленного корня в бандле по-прежнему не доказана | сборка проходит, корректность корня после бандла не доказана: в рантайме web `MEDIA_DIR` и путь `.env` могут уехать в каталог сборки | открыто: проверить корень в бандле и дать корню опору, не зависящую от `import.meta.url` - E0-10/E0-11 |
| 06.09.2026 | E0-02, ревью | `packages/core/test/queues.test.ts` (переехал из `apps/worker` в E0-04) | регекс имён очередей `/^[a-z]+\.[a-z-]+$/` отвергает цифры | легальное имя вида `analytics.ingest-v2` покрасит канарейку | закрыто: E0-08 (регекс `/^[a-z]+\.[a-z0-9-]+$/` и канарейка состава в `packages/core/test/queues.test.ts`) |
| 06.09.2026 | E0-02, ревью | `packages/db/package.json`, `apps/api`, `apps/worker` | скрипт `test:int` есть только в `packages/db`; в api/worker интеграционного контура нет | будущие int-тесты api/worker некуда положить без правки конфигов | закрыто: api - E0-06, worker - E0-08 (`apps/worker/vitest.int.config.ts`, скрипт `test:int`, `test/helpers.int.ts` и четыре `*.int.test.ts` против живых PG/Redis) |
| 06.09.2026 | E0-02, ревью | `.github/workflows/ci.yml` | `pnpm test:int` не подключён ни к одному конвейеру, сервиса Postgres в CI нет | контур гниёт незаметно, регресс интеграции ловится только локально | открыто: добавить сервис и шаг - E0-12 |
| 06.09.2026 | E0-02, ревью | `packages/core/vitest.config.ts`, `packages/db/vitest.config.ts`, `apps/api/vitest.config.ts`, `apps/worker/vitest.config.ts`, `packages/integrations/youtube/vitest.config.ts`, `packages/contracts/vitest.config.ts`, `packages/api-client/vitest.config.ts` | семь почти одинаковых конфигов (четвёртый добавлен в E0-04, пятый в E0-05, шестой и седьмой в E0-07), общий пресет vitest не заведён (Vitest 2.1.9 без `projects`) | каждая правка тестовой политики - в семь мест | открыто: общий пресет - E0-12 |
| 06.09.2026 | E0-02, ревью | `packages/db/vitest.int.config.ts` | параллелизм файлов не ограничен (`fileParallelism`) | со второго int-теста в `packages/db` два воркера пойдут в одну тестовую базу одновременно | закрыто: E0-04 (`fileParallelism: false` в `packages/db/vitest.int.config.ts`) |
| 06.09.2026 | E0-02, добор | `turbo.json:test` | `^test` тянет в хеш только зависимости, у которых есть скрипт `test`; у `packages/integrations/*` его нет (у `@sf/db` скрипт появился в E0-04, дыра по нему закрыта) | правка `packages/integrations/*` не сбросит кэш `api#test`/`worker#test`, когда приложения начнут импортировать клиентов YouTube/Gemini/TTS | переадресовано E0-12 (E0-06): предмет есть уже сегодня - `apps/worker/package.json` держит `@sf/gemini` и `@sf/tts` в `dependencies`, то есть правка любого из них не сбросит хеш `@sf/worker#test`; адрес именно E0-12 потому, что закрыть дыру можно только правкой конвейера - скриптом `test` у пакета без тестов (это `--passWithNoTests`, запрещён) или сменой графа задач `turbo.json`, то есть вместе с `:24`, `:27`, `:28`, `:29` и общим пресетом vitest; для `@sf/youtube` закрыто E0-05 (скрипт `test` появился), у `@sf/gemini` и `@sf/tts` дыра остаётся |
| 06.09.2026 | E0-02, добор | `tsconfig.base.json` | корневой файл не входит в хеш задачи `typecheck` ни одного пакета | ослабление strict в нём даёт FULL TURBO зелёный без проверки | открыто: E0-12 |
| 06.09.2026 | E0-02, добор | `package.json:devDependencies.turbo` | диапазон `^2.3.0` при том, что `$TURBO_ROOT$` в `inputs` требует turbo 2.10; на 2.3-2.9 это обычный glob, который молча ничего не матчит | дыра в хеше `.env.test` на старых минорных версиях turbo | открыто: поднять нижнюю границу диапазона (решение техлида) - E0-12 |
| 06.09.2026 | E0-02, добор | `turbo.json:test.inputs` | `$TURBO_ROOT$/.env.test` и `$TURBO_ROOT$/.env.example` инвалидируют тесты всех пакетов, включая `@sf/core#test`, которому ни фикстура, ни шаблон не нужны | лишние cache miss на правку `.env.test` и `.env.example` | открыто: E0-12 |
| 06.09.2026 | E0-03, перепроверка | `packages/config/src/env-file.ts:6,17` | тип `EnvLike` и чистый предикат `isEnvValueSet` лежат в модуле файлового ввода, поэтому чистый `load-env.ts` импортирует их из загрузчика | чистый парсер не отделить от файлового слоя (edge-runtime без `node:fs`) без переноса примитива; переезд задевает семь файлов ради имени модуля | открыто: первая задача, которая тронет `env-file.ts` |
| 06.09.2026 | E0-03, ревью | `turbo.json:7` | у задачи `build` не объявлен ни один env, и `.env` не входит в `inputs` ни одной задачи | когда E0-10 импортирует `@sf/config/server` в серверный компонент, `next build` в CI останется без `DATABASE_URL`; правка `.env` не сбрасывает кэш ни одной задачи | открыто: E0-10/E0-12 |
| 06.09.2026 | E0-03, ревью | `.claude/hooks/rules.sf.json:107` | `skipIf` у S11 снимается подстрокой `server-only` в любом месте строки: `import { env } from "@sf/config"; // server-only` проходит замок | клиентский импорт с таким комментарием уйдёт в дифф без замка, а гейта `build` нет; в чанк попадут `ADMIN_PASSWORD`, `TOKEN_ENCRYPTION_KEY` и ключи провайдеров | открыто: E0-01A (правка правил у владельца) |
| 06.09.2026 | E0-03, ревью | `packages/db/drizzle.config.ts:7` | `process.env.DATABASE_URL ?? ""` в обход `@sf/config` | README обещает, что `cp .env.example .env` достаточно, но `pnpm db:migrate` корневой `.env` не читает | закрыто: E0-04 (`dbCredentials` убран - из конфига идёт только `generate`; `db:migrate` = `src/cli/migrate.ts`, читает `env.DATABASE_URL` из `@sf/config`) |
| 06.09.2026 | E0-03, ревью | `packages/config/test/env-example.test.ts` | сверка примера и схемы односторонняя: лишний или опечатанный ключ примера не ловится, а «документированной» считается любая строка, у которой текст до `=` совпал с именем ключа (включая комментарий) | ключ добавили в схему, в примере оставили строку `# KEY пока не нужен` - тест зелёный, переменной в примере нет | открыто: E0-12 |
| 06.09.2026 | E0-03, ревью | `apps/web/package.json`, `apps/web` | зависимость от `@sf/config` при нуле импортов до E0-10; постоянного автотеста на клиентский импорт нет (у `apps/web` нет vitest-обвязки, гейт `build` выключен) | мёртвая проводка, если потребитель не появится; запрет клиентского импорта держится на разовой ручной сборке | открыто: E0-10 |
| 06.09.2026 | E0-03, перепроверка | `packages/config/src/server.ts:14` | подвход реэкспортирует весь `env` целиком, включая секреты, хотя web из него нужен только `API_INTERNAL_URL` (решение PM Р5) | серверный компонент, передавший `env` или спред в клиентский, сериализует секреты в RSC-payload; `server-only` этого не ловит - маркер на модуле, а не на значении | открыто: сужающее представление `webEnv` - E0-10 |
| 06.09.2026 | E0-03, перепроверка | `.gitignore:5` | игнорируется только точное имя `.env` | `.env.production`, `.env.vps`, `.env.bak` попадут в коммит с настоящими ключами | открыто: до первого деплоя - E0-11 |
| 06.09.2026 | E0-03, перепроверка | корень репозитория | `.dockerignore` отсутствует, а загрузчик ждёт `.env` в корне репозитория | первый же `COPY . .` в E0-11 запечёт настоящий `.env` владельца в слой образа | открыто: E0-11 |
| 06.09.2026 | E0-03, перепроверка | `packages/config/src/env-file.ts:bootstrapEnv` | значения файла кладутся в глобальный `process.env`, который целиком наследуют дочерние процессы | `spawn` ffmpeg в E6 получит все ключи провайдеров; нужен явный `env` у `spawn` | открыто: E6 |
| 06.09.2026 | E0-03, перепроверка | `packages/config/package.json` (условие `browser` корневого входа) | условие `browser` сработает и в vitest с `environment: "jsdom"` | первый компонентный тест web упрётся в `server-only` в рантайме, хотя импорт легальный | открыто: E0-10 |
| 06.09.2026 | E0-03, перепроверка | `docs/adr/0001-config-server-entry.md`, `docs/30_E0_TASKS.md:37` (решение 2) | ADR фиксирует, что `@sf/config/server` не работает в edge-runtime, а решение 2 требует basic-auth в `middleware.ts` | middleware не сможет прочитать `ADMIN_PASSWORD` из конфига - потребуется другой источник или Node-runtime | открыто: E0-10 |
| 06.09.2026 | E0-03, перепроверка круга 4 | `apps/worker` (реестр очередей) | `WORKER_CONCURRENCY` - верхняя граница на очередь, а не сумма по процессу: при 14 очередях `10` даёт до 140 параллельных джоб, и платные fan-out очереди (`intel.analyze-video`, `research.run`, `script.generate`, `tts.generate`) ничем не отличаются от дешёвых | пачка платных вызовов уходит раньше, чем дневной кап успевает сработать | сужено: ресурсная часть закрыта E0-08 (`apps/worker/src/lib/job-policy.ts`: `queueConcurrency` = `min(env.WORKER_CONCURRENCY, 2)` для пяти платных fan-out очередей; посадка `apps/worker/test/job-policy.test.ts`), бюджетная остаётся: потолок конкурентности - не дневной кап, пачка из двух платных вызовов всё так же уходит раньше, чем сработает `BudgetGuard` - E0-09 |
| 06.09.2026 | E0-03, перепроверка круга 4 | `packages/config/src/load-env.ts` (дефолт `NODE_ENV`) | дефолт `production` живёт только в разобранном `env`; `process.env.NODE_ENV` на машине без объявленной переменной остаётся пустым | цена: наш код работает как production, а сторонние библиотеки api/worker, читающие `process.env.NODE_ENV` напрямую, идут по своей dev-ветке (лишние проверки, другой формат логов, dev-предупреждения) | принято (решение техлида 06.09): запись в `process.env.NODE_ENV` ломает `next build` под `transpilePackages`. Переоткрыть, если появится зависимость api/worker, чья ветка по `NODE_ENV` меняет ответ или лог - проверяется в E0-06 и E0-08. Проверено на E0-06: во всём рантайм-дереве `apps/api` (157 пакетов) `process.env.NODE_ENV` читают только `bullmq` (`child-processor.js`, таймаут ответа sandbox-процессора; api его не использует) и `react` (транзитивный peer `drizzle-orm`, в api не грузится) - ни ответа, ни формата лога это не меняет; fastify, pino, ioredis, zod, drizzle-orm, postgres не ветвятся вовсе. Проверено на E0-08: в рантайм-дереве `apps/worker` (123 пакета) `process.env.NODE_ENV` читают только `bullmq` (`child-processor.js`, sandbox-процессоры - воркер их не использует, обработчики передаются функцией) и `react` (транзитивный peer `drizzle-orm`, в воркере не грузится); ни ответа, ни формата лога это не меняет. Остаётся принятым; переоткрыть, если появится зависимость с веткой по `NODE_ENV`, меняющей ответ или лог |
| 06.09.2026 | E0-03, перепроверка круга 4 | `packages/config/src/env-file.ts` (`mergeEnvFile`) | ключи файла кладутся в `target` без сверки со схемой: `.env` со строкой вне схемы (`FFMPEG_PATH`, `HTTP_PROXY`) оседает в `process.env` мимо валидации | цена: значение вне схемы не проходит проверок конфига и не видно в `env`; ключ, которому нельзя попадать в `process.env`, попадёт туда молча | принято: поведение намеренное и описано в коде, совместимо с прежним `dotenv`; фильтр по схеме лишил бы `.env` права настраивать библиотеки и дочерние процессы. Переоткрыть, если появится ключ, которому нельзя попадать в `process.env` |
| 06.09.2026 | E0-03, перепроверка круга 4 | `packages/db/src/index.ts:1` | импортирует `@sf/config`, тогда как §6 System Design задаёт направление `db -> core` | пакет данных тянет чтение окружения; тесты и потребители `@sf/db` получают парсинг env, который им не нужен | закрыто: E0-04 - не «убрали `@sf/config`», а провели границу внутри пакета: библиотека (`client`, `migrate`, `seed`, `repos`, `schema`) конфигурации не знает и принимает url/db параметром, ребро `db -> config` осталось только у точек входа `src/cli/**` (ADR 0002, строка в `10_SYSTEM_DESIGN.md` §6) |
| 06.09.2026 | E0-03, перепроверка круга 4 | `packages/config/src/schema.ts` (`YT_UNITS_DAILY_SOFT_CAP`) | порты и конкурентность границу получили, дневной кап квоты YouTube остался без `.max()` | опечатка `80000` вместо `8000` молча снимает квотный предохранитель: суточная квота Data API выжигается до срабатывания капа | открыто: E0-09 |
| 06.09.2026 | E0-03, перепроверка | `apps/web/next.config.ts:13` | `experimental.extensionAlias` объявлен глобально для всей сборки, включая `node_modules`; `.js` стоит последним, но пакет, у которого рядом с `foo.js` лежит `foo.ts`, начнёт компилироваться из исходника | чужой пакет собирается из исходника вместо опубликованного `.js`: неожиданные ошибки типов и разный результат сборки | открыто: E0-10 |
| 06.09.2026 | E0-03, перепроверка круга 5 | `.env.example:19,21,30` | один и тот же порт api повторён в трёх переменных (`API_PORT`, `API_INTERNAL_URL`, `GOOGLE_OAUTH_REDIRECT_URL`) и держится только комментарием: межполевой сверки нет, хотя механизм в пакете уже есть (производная схема `load-env.ts`) | `API_PORT=4000` без правки двух других: web ходит на 3001 и получает ECONNREFUSED в рантайме, Google отвечает `redirect_uri_mismatch` - вместо ошибки конфигурации на старте | открыто: E0-10, вместе с пересмотром самого правила (решение техлида 08.09: в Compose `API_INTERNAL_URL=http://api:3001`, а `GOOGLE_OAUTH_REDIRECT_URL` - публичный URL за прокси без порта api, так что прямая сверка отказала бы на верной конфигурации) |
| 06.09.2026 | E0-03, перепроверка круга 5 | `packages/config/src/limits.ts` | деньги хранятся как `number` (double): `geminiDailyBudgetUsd`, `ttsMonthlyBudgetUsd` | к E13 те же величины лягут в `api_usage_log`/`api_usage_daily`, где нужен `numeric`; сложение float в guard даёт расхождение с БД и «не сработавший» кап на границе | открыто: E13 (тип `numeric` в схеме БД и решение о сложении в guard). Дополнено E0-04: `sumCostUsdToday`/`ThisMonth` возвращают `number` (сумма считается в Postgres, в double уходит одно число) - тип денег (`limits` + `cost_usd` + агрегаты) пересматривается целиком в E13 вместе с `api_usage_daily` |
| 06.09.2026 | E0-04 | `packages/db/package.json:db:generate` | `drizzle-kit@0.28.1` резолвит `require` буквально и падает на NodeNext-импорте `./radar.js` в `src/schema/index.ts` (`MODULE_NOT_FOUND`); скрипт запускает бинарник через `tsx ./node_modules/drizzle-kit/bin.cjs` | генерация миграций держится на прямой ссылке на файл внутри `node_modules` и на том, что `tsx` подменяет загрузчик; смена раскладки pnpm или версии drizzle-kit ломает `db:generate` | открыто: проверить поддержку `.js`->`.ts` в свежем drizzle-kit и вернуть обычный вызов - E0-12 |
| 06.09.2026 | E0-04 | `packages/db/src/schema` (`tracked_channel.updated_at`, `idea.updated_at`, `dub_track.updated_at`, `analytics_daily.updated_at`) | колонки есть, автообновления нет: ни триггера, ни `$onUpdate` (в E0 в эти таблицы никто не пишет) | первый же `UPDATE` из E1/E3/E7 оставит `updated_at` временем вставки, и «когда менялось» станет неверным во всех витринах; у `analytics_daily` это прямо ломает признак «день ещё уточняется» | открыто: первая задача, которая начнёт писать в эти таблицы - E1-01/E3-02/E7 (`analytics.ingest` ставит `updated_at` в ветке `ON CONFLICT`) |
| 06.09.2026 | E0-04 | `packages/db/src/schema/radar.ts:story_cluster_video`, `idea-flow.ts:idea_source_video` | связующие таблицы без первичного ключа: уникальность пары держит уникальный индекс (`cluster_video_uq` и `idea_source_video_uq`, второй добавлен по ревью E0-04), но PK у таблиц нет | ORM и репликация опираются на PK; строку нельзя адресовать в `UPDATE`/`DELETE` иначе как парой колонок | открыто: E3-02 (там `idea_source_video` всё равно переделывается) |
| 06.09.2026 | E0-04 | локальное окружение владельца | `pnpm test:int` не запускается: хостовые порты 5432 и 6379 заняты контейнерами другого проекта (`books_postgres`, `books_redis`), `docker compose -f infra/docker-compose.yml up -d` падает на `port is already allocated`, а `.env.test` ждёт базу на `localhost:5432` | интеграционный контур `packages/db` (миграция, индексы, enum, агрегаты, seed) не прогонялся ни разу; DoD E0-04 в части «`*.int.test.ts` зелёные» не подтверждён | закрыто: E0-04 (решение техлида Р6 - Compose публикует Postgres на `127.0.0.1:5442`, Redis на `127.0.0.1:6389`; `.env.test`, `.env.example`, канарейка api и README следом; `pnpm test:int` прогнан) |
| 06.09.2026 | E0-04, ревью | `packages/db/src/cli/migrate.ts`, `src/cli/seed.ts` | точка входа читает `env` из `@sf/config`, а он валидирует окружение целиком: без `REDIS_URL` и `ADMIN_PASSWORD` миграция не стартует, хотя ей нужен только `DATABASE_URL` | одноразовому сервису миграций в Compose нужны все обязательные переменные, иначе ZodError по `ADMIN_PASSWORD` до первого соединения | открыто: E0-11 (либо все переменные сервису, либо узкая схема для CLI) |
| 06.09.2026 | E0-04, ревью | `packages/db/src/repos/api-usage-log.ts` (`insert` и три агрегата) | провайдер - свободный `string` в сигнатурах репозитория: опечатанное имя неотличимо от «расхода не было» и молча даёт `0` | `BudgetGuard` зовёт `sumUnitsToday(db, "youtube-data")` вместо `"youtube_data"`, видит 0 из 10 000 units и не срабатывает | закрыто: E0-05 (`Provider` из `@sf/core` в `insert` и трёх агрегатах; посадка - `packages/db/test/usage-provider.test.ts`) |
| 06.09.2026 | E0-04, ревью | `packages/db/migrations/0000_core.sql` (`idea.card`, `research_brief.*`, `script.*`, `production_package.*`) | артефакты LLM лежат в JSONB без `model`/`prompt_version`/`cost_usd`/`schema_version` рядом; требование hard rules соблюдает только `video_analysis` | после E3-E5 не ответить, каким промптом и моделью сделан скрипт и сколько он стоил; `experiment.recompute` не разложит результат по версии промпта | открыто: E3-E5 (колонки версионирования в таблицы-владельцы вместе с первым пишущим кодом) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/package.json` | `@sf/config` в `dependencies` пакета, тогда как ADR-0002 разрешает его только в `packages/db/src/cli/**`, `packages/db/test/**` и `packages/db/vitest*.config.ts` | автоматическая проверка направления зависимостей по манифестам увидит ребро `db -> config` у библиотеки | открыто: E0-12 (проверка §6 по манифестам с исключением для `packages/db/src/cli/**`, `packages/db/test/**` и `packages/db/vitest*.config.ts` - ровно та граница, что в ADR-0002) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/src/seed.ts` (`radar.weights`) | значение типизировано голым `$inferInsert`, форма описана комментарием, хотя `TrendWeights` есть в `@sf/core` | переименование поля в E1-05 оставит seed со старым ключом, обнаружится в рантайме | открыто: E1-05/E1-06 (типизировать значение `TrendWeights`) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/src/schema/radar.ts` (`snapshot_video_point_uq`) | `UNIQUE (video_id, point)` при значении `adhoc` в enum: ад-хок снапшот возможен только один на видео за всю жизнь строки | второй ручной «refresh stats now» из UI получит нарушение уникальности либо молча погасится `onConflictDoNothing` | открыто: E1-03 (частичный индекс `WHERE point <> 'adhoc'` отдельной миграцией) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/test/migrate.int.test.ts` | идемпотентность второго `runMigrations` подтверждается `toHaveLength`, а не сравнением состава таблиц | порча состава вторым проходом с тем же количеством таблиц не поймается | открыто: E0-12 |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/test/helpers.int.ts`, `test/connection.int.test.ts` | имя `helpers` не говорит, что внутри фикстура тестовой базы; в `connection.int.test.ts` второй литерал `shorts_factory_test` рядом с тем, что в хелпере | следующий хелпер ляжет в тот же файл; переименование тестовой базы - правка в двух местах | открыто: E0-12 (вместе с общим пресетом vitest) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/src/cli/migrate.ts`, `src/cli/seed.ts` | команды молчат при успехе: ни числа применённых миграций, ни «вставлено/пропущено» | оператор не отличает «сработало» от «ничего не сделало», проверка только через psql | открыто: E0-11 (вывод точек входа вместе с сервисом миграций) |
| 06.09.2026 | E0-04, ревью (придирка) | `packages/db/src/repos/api-usage-log.ts` (`periodWindow`) | единица периода параметризуется как значение (`date_trunc($1::text, …)`), а не как идентификатор: работает, инъекции нет (оба литерала внутренние), но конструкция неочевидна | цена только в поддержке: следующий читатель проверяет безопасность заново | открыто: E0-09 (первая задача, которая тронет агрегаты) |
| 06.09.2026 | E0-04, ревью | `packages/db/src/client.ts`, `src/migrate.ts` | TLS не требуется и не проверяется: защита соединения держится только на `?sslmode=` внутри `DATABASE_URL` | на VPS соединение с базой может молча пойти без шифрования | открыто: E0-11 (вместе с `db-guard`) |
| 06.09.2026 | E0-04, ревью | `packages/db/package.json` | пакет не объявляет `exports`: `@sf/db/src/cli/migrate.js` (а с ним весь `env`) импортируется откуда угодно, ловит только замок S11 по имени | точка входа с чтением конфигурации доступна web-коду и любому пакету | открыто: E0-11, вместе с `:66` (публичную поверхность `@sf/db` задавать один раз - с `exports` и реэкспортом `runMigrations`/`seedAppSettings`) |
| 06.09.2026 | E0-04, ревью | `packages/db/src/index.ts` | `runMigrations` и `seedAppSettings` не реэкспортированы: программный доступ только относительным путём | сервис миграций и тесты E0-11 импортируют внутренности пакета | открыто: E0-11 |
| 06.09.2026 | E0-04, ревью | `packages/db/src/schema/system.ts` (`oauth_token.encrypted_refresh_token`) | один `text` без nonce/версии ключа и без `CHECK` на формат; формат «iv:tag:ct» остаётся негласным договором | смена ключа или формата шифрования не отличима от испорченной строки | открыто: E7-01 |
| 06.09.2026 | E0-04, ревью | `packages/db/migrations/0000_core.sql` (блок `CREATE TYPE`) | типы создаются без `IF NOT EXISTS`/`DO $$`, в отличие от таблиц и индексов | применение на базе с ручной схемой падает целиком на первом же типе | открыто: E0-12 (проверка миграций на непустой базе) |
| 06.09.2026 | E0-04, ревью | `.env.example` (`DATABASE_URL`) | хост `localhost` не годится изнутри контейнеров api/worker - им нужен `postgres:5432` | первый запуск полного стека в Compose упрётся гарантированно | открыто: E0-11 |
| 06.09.2026 | E0-04, ревью | `packages/db/src/repos/api-usage-log.ts` (`sumWindow`) | функция получает и колонку, и её имя, хотя имя выводится из колонки | при третьем агрегате аргументы разъедутся, ошибка будет в тексте сообщения | открыто: E0-09 |
| 06.09.2026 | E0-04, решение Р8 (PM) | `packages/db/src/seed.ts` (`queues.enabled`), `docs/DECISIONS.md:75` | все очереди реестра засеяны `true` (26 на момент решения, 29 после E0-08 - три служебные); условие, на котором это принято, - каждый эпик с обработчиком платной или публикующей очереди приносит свой предохранитель в той же задаче; что означает `false` в рантайме и где E0-08 читает ключ - вопрос техлиду остался открытым | эпик заводит платный обработчик, надеясь на переключатель, которого никто не читает: очередь включена по умолчанию, предохранителя нет; оператор считает `false` рабочим выключателем | закрыто: E0-08 (решение техлида 10.09: `false` = пауза очереди BullMQ, ресинк раз в минуту в `runtime.ts`, ключ читает только `apps/worker/src/lib/queue-switches.ts`; `docs/adr/0003-queue-switches.md`). Условие PM - предохранитель в той же задаче - остаётся действующим для каждого эпика с платной или публикующей очередью |
| 06.09.2026 | E0-04, ревью круга 2 (придирка) | `packages/db/src/repos/api-usage-log.ts:47` (`toTotal`) | `value === undefined` схлопнут в одну ветку с `null`: структурная аномалия (агрегат без ожидаемого поля) даёт «расход 0», хотя на неожиданный тип соседняя ветка бросает | переименование алиаса `total` при рефакторинге даст не ошибку, а «потрачено 0» во всех трёх агрегатах | открыто: E0-09 |
| 06.09.2026 | E0-04, ревью круга 2 (придирка) | `packages/db/src/migrate.ts` (`runMigrations`) | нет advisory-lock, а drizzle читает журнал до транзакции: два одновременных раннера оба видят пустой журнал | сервис миграций Compose стартует, пока разработчик делает `pnpm db:migrate`: второй падает на `CREATE TYPE` без `IF NOT EXISTS`, контейнер выходит с ненулевым кодом | открыто: E0-11 |
| 06.09.2026 | E0-04, ревью круга 2 (придирка) | `packages/db/test/helpers.int.ts:41` против `:69` | `postgres(env.DATABASE_URL, { max: 1 })` собирается в файле дважды: `resetTestDatabase` не зовёт свой же `openTestSql()` | опция клиента (например `onnotice`, гасящая NOTICE от `DROP SCHEMA … CASCADE`) ляжет в одно место из двух | открыто: E0-12 |
| 06.09.2026 | E0-04, ревью круга 2 (придирка) | `packages/db/test/local-host-guard.ts` | страж закрывает хост, но не порт, и разбирает строку WHATWG-`URL`, тогда как соединение открывает postgres.js своим парсером | остаётся узкий путь: локальный порт, проброшенный туннелем на удалённый Postgres с базой `shorts_factory_test`; расхождения парсеров проверены - каждое даёт отказ | открыто: E0-12 (вместе с проверкой хоста и порта в CI) |
| 06.09.2026 | E0-04, ревью круга 2 | `packages/db/migrations/0000_core.sql` (`analytics_uq`), `.github/workflows/ci.yml` | `UNIQUE NULLS NOT DISTINCT` требует Postgres >= 15; стек §2 фиксирует PG16, но версия сервиса Postgres в CI (E0-12) нигде не названа | на `postgres:14` миграция 0000 упадёт на синтаксисе, и это будет выглядеть как поломка миграции, а не как версия сервиса | открыто: E0-12 (зафиксировать `postgres:16` в services) |
| 06.09.2026 | E0-04, ревью круга 2 | `packages/db/src/schema/intelligence.ts` (`video_analysis.dna`) | таблица несёт `model`/`prompt_version`/`cost_usd`, но не `schema_version` для JSONB `dna`; строка про артефакты LLM в JSONB покрывает только `idea`/`research_brief`/`script`/`production_package` | смена формы `ContentDnaSchema` в E2 сделает старые строки нечитаемыми молча: отличить «старая версия схемы» от «битые данные» нечем | открыто: E2 (вместе с первым пишущим кодом DNA) |
| 06.09.2026 | E0-04, ревью круга 2 | `packages/db/src/schema` (`tracked_video`, `story_cluster_video`, `idea_source_video`) | у трёх таблиц нет `created_at` | не ответить, когда видео попало в отслеживание и когда источник привязали к идее; отладка синка и Inbox идёт по соседним таблицам | открыто: E1-01/E3-02 |
| 06.09.2026 | E0-04, ревью круга 2 | `packages/db/src/repos/api-usage-log.ts:24-28` (док-комментарий `periodWindow`) | комментарий называет окно «half-open-in-spirit», фактически закрыты оба конца (`>= start` и `<= end`) | при склейке соседних окон в `system.usage-rollup` (E13) строка ровно на границе попадёт в оба окна и будет посчитана дважды | открыто: E13 |
| 06.09.2026 | E0-04, ревью круга 2 | `packages/db/test/helpers.int.ts:64` (`openTestDb`) | вызывается на верхнем уровне тестовых файлов, до `resetTestDatabase()` | работает только потому, что postgres-js соединяется лениво; фикстура, которой понадобится живое соединение на импорте, сломается неочевидно | открыто: E0-12 |
| 06.09.2026 | E0-04, ревью круга 2 | `apps/worker/test/queues.test.ts:18` | канарейка сравнивает `QUEUE_NAMES.length` с числом, которое точка входа печатает из того же `QUEUE_NAMES` | ловит возврат локальной копии другой длины, но не расхождение состава при равной длине | закрыто: E0-08 (канарейка сравнивает состав, а не длину: `apps/worker/test/queues.test.ts` по реестру процессоров и `packages/core/test/queues.test.ts` по служебным очередям) |
| 06.09.2026 | E0-04, ревью круга 2 | `infra/docker-compose.yml:6-7` | `POSTGRES_USER`/`POSTGRES_PASSWORD: sf` литералом (строка не из диффа E0-04); спасает только привязка публикации к `127.0.0.1` | переиспользование файла на VPS в E0-11 отдаст базу с паролем из репозитория | открыто: E0-11 (вынести в env вместе с деплоем) |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/src/repos/api-usage-log.ts` (`periodWindow`), `src/repos/*.ts` (сигнатуры) | `now()` - это `transaction_timestamp()`: внутри `db.transaction` верхняя граница окна замирает на моменте старта транзакции; отдельно репозитории принимают только `Db`, не `PgTransaction`, поэтому вызвать их внутри транзакции сейчас нельзя | `BudgetGuard` E0-09, вызванный из джобы внутри `db.transaction`, не увидит строк, закоммиченных после старта его транзакции: заниженная сумма, вызов уходит за капом | открыто: E0-09 (`clock_timestamp()` или снятие верхней границы при незаданном `now`, вместе с поддержкой `PgTransaction` в сигнатурах репозиториев) |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/src/repos/app-setting.ts` (`set`), `src/seed.ts` (`excluded.value \|\| app_setting.value`) | слияние в seed корректно только когда в строке лежит JSONB-объект, а `set` принимает `unknown` и значение не валидирует: для скаляра или массива Postgres по правилу `jsonb \|\| jsonb` заворачивает оба операнда в массив | в `queues.enabled` кладут скаляр - следующий `db:seed` молча пишет `[{…26 ключей}, true]`, реестр читает каждый переключатель как `undefined`; отказа нет, в логе пусто | открыто: E1-10 (валидация значения в `appSettingRepo.set`, объект для ключей-наборов) |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/src/cli/seed.ts` | порядок «migrate -> seed» держится только на README: `db:seed` на чистой базе падает сырым `PostgresError: relation "app_setting" does not exist` | оператор на чистой базе набирает `pnpm db:seed` первым и получает стек-трейс драйвера вместо подсказки «сначала `pnpm db:migrate`» | открыто: E0-11 (подсказка в точке входа вместе с сервисом миграций) |
| 06.09.2026 | E0-04, ревью круга 3 (дополнено в круге 6) | `packages/db/src/seed.ts` (`queues.enabled`, `radar.weights`) | набор ключей растёт монотонно для обеих JSONB-строк, не только для очередей: слияние дописывает ключи, но никогда не удаляет - переименованная или снятая очередь, как и переименованный вес, оставляют свой ключ навсегда | оператор видит живой на вид переключатель мёртвой очереди и выключает им то, чего нет; переименование веса в `TrendWeights` (E1-05) оставит в базе старый ключ `radar.weights` мусором рядом с новым | сужено: `queues.enabled` закрыт E0-08 (`pruneQueueSwitches` в `packages/db/src/seed.ts` удаляет ключи вне `QUEUE_NAMES`, не трогая `updated_at`, когда удалять нечего; посадка в `packages/db/test/app-setting.int.test.ts`); остаётся `radar.weights` - E1-05 (чистка ключей вне актуальной формы `TrendWeights`) |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/test/helpers.int.ts:31-35` | страж имени тестовой базы (`current_database() = 'shorts_factory_test'`) остался без отрицательного прогона: `assertLocalHost` проверяется юнитом, а этот - ничем | снятое или испорченное условие имени базы уйдёт в дифф зелёным, а цена ошибки - `DROP SCHEMA` в чужой базе | открыто: E0-12 (вынести страж в чистый модуль и покрыть, как `local-host-guard`) |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/src/schema/idea-flow.ts` (`script.status`) | enum `draft\|final` без частичного уникального индекса «ровно один `final` на идею и язык» | инвариант держится только кодом: два финальных скрипта на одну идею - и производство берёт случайный | открыто: E5-03 |
| 06.09.2026 | E0-04, ревью круга 3 | `packages/db/src/repos/api-usage-log.ts:107` (`insert`) | запрет на явный `createdAt` нигде не был записан: поле входило в `$inferInsert`, и логгер E0-09 естественным образом поставил бы штамп с часов своего процесса | строка с ушедшими вперёд часами не попадала бы в окно агрегатов: guard видит расход меньше реального и пропускает вызов за капом | закрыто: E0-04 (тип входа `Omit<typeof apiUsageLog.$inferInsert, "createdAt">`; посадка - `@ts-expect-error` в `test/api-usage-log.int.test.ts` и `pnpm typecheck`) |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/src/seed.ts:46-55` (`seedAppSettings`) | слияние `excluded.value \|\| app_setting.value` в общем `onConflictDoUpdate` идёт одним стейтментом по всем строкам `DEFAULT_APP_SETTINGS`, а не только по объектным значениям; `jsonb \|\| jsonb` над скаляром или массивом заворачивает оба операнда в массив, не берёт нужный | ближайшие скалярные ключи эпика E1 (`radar.sync_interval_min`, `radar.snapshot_tick_min`, `radar.backfill_count`, `radar.cluster.window_h`) при повторном `pnpm db:seed` молча превратятся в массив вместо числа, без отказа; строка про валидацию в `appSettingRepo.set` (E1-10) закрывает путь через `set`, путь через `seed` остаётся открытым | принято: в E0-04 `DEFAULT_APP_SETTINGS` содержит только объектные значения (`radar.weights`, `queues.enabled`), сценарий не воспроизводится. Переоткрыть, как только E1-06 добавит в `DEFAULT_APP_SETTINGS` хотя бы один скалярный или массивный ключ - тогда развести стратегию слияния по типу значения или завести отдельный `onConflictDoNothing` для таких ключей |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/src/seed.ts:47` (`excluded.value \|\| app_setting.value`) | слияние не отличает «дефолт, который seed сам записал раньше» от «значение, которое выбрал оператор»: правило «сохранённое побеждает» одинаково защищает оба случая | E1-06 меняет стартовое значение веса (например `baselineRatio: 0.2` на `0.25`) - на уже засеянной базе, где оператор его не трогал, новое значение дефолта не дойдёт: там лежит старое число, неотличимое от выбора оператора | открыто: E1-06 (нужен способ различить «дефолт» и «выбор оператора» - отдельная колонка/таблица версий дефолтов или явный флаг) |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/src/schema/own-channel.ts:90-108` (`dub_track`) | у таблицы есть `updated_at`, но нет `created_at`; существующая строка про отсутствие `created_at` называет только `tracked_video`, `story_cluster_video`, `idea_source_video` | нельзя ответить, когда дорожка озвучки создана, только когда обновлена последний раз; отладка `tts.generate` и публикации идёт по соседним таблицам | открыто: E9/E10 |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/src/repos/api-usage-log.ts:116-121` (`insert`) | `Omit<typeof apiUsageLog.$inferInsert, "createdAt">` закрывает только один из двух генерируемых БД столбцов; `id` (`serial`) остаётся в допустимом входном типе | вызывающий может явно подать `id` в обход последовательности `serial`; коллизия с автоинкрементом обнаружится только на вставке | открыто: E0-09 (сузить тип входа `insert` до `Omit<typeof apiUsageLog.$inferInsert, "id" \| "createdAt">`) |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/test/api-usage-log.int.test.ts:206` | `expect(await sumUnitsToday(db, provider)).toBe(0)` без `now` полагается на то, что системная дата машины не совпадает с зафиксированной в тесте датой прошлого (15 марта) | в день, когда системная дата и есть 15 марта, вставленная строка попадает в окно «сегодня» и тест краснеет без связи с регрессией кода | открыто: E0-12 (общий пресет vitest даёт фиксированное «сегодня» int-тестам, которые проверяют границу дня) |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/test/app-setting.int.test.ts` | один `resetTestDatabase()` в `beforeAll` на весь файл; кейсы `seedAppSettings` читают состояние, оставленное предыдущими кейсами (`run twice` полагается на seed из предыдущего теста, `does not undo...` - на предыдущий `seed`/`set`) | порядок тестов внутри файла стал частью контракта; `--shuffle` или фильтр по имени тихо ломает проверки, которые выглядят независимыми | открыто: E0-12 (вместе с общим пресетом vitest и стражем порядка тестов) |
| 06.09.2026 | E0-04, финальное ревью, круг 6 | `packages/db/src/cli/seed.ts:14-18` | `closeDb(db)` в `finally` без перехвата: если `seedAppSettings` бросает и `closeDb` тоже бросает (соединение уже оборвано), исходная ошибка seed теряется - наружу уходит ошибка закрытия | оператор видит причину сбоя закрытия клиента вместо настоящей причины падения seed (например нарушение constraint); диагностика идёт по ложному следу | открыто: E0-11 (та же форма нужна `src/cli/migrate.ts`) |
| 07.09.2026 | E0-05, решение Р1 | `apps/worker` (постановка джобов), `packages/core/src/domain/job-ids.ts` | формат `jobId` держится юнит-тестом core, но никто не проверяет его настоящим BullMQ: `Job.addJob → validateOptions` отвергает custom id с `:` | смена склейки или новый билдер с `:` внутри уронит постановку джоба в рантайме, а не в тесте | закрыто: E0-08 (`apps/worker/test/job-id.int.test.ts` прогоняет `jobIds.*` через настоящий `queue.add`; `jobId()` отвергает id из одних цифр, `jobIds.dlqEntry` кодирует `:`) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/usage.ts`, `packages/db/src/repos/api-usage-log.ts` | `ApiUsageEntry` не является входом `apiUsageLogRepo.insert`: `costUsd` - `number` против колонки `numeric` (`$inferInsert` ждёт `string`), а под `exactOptionalPropertyTypes` четыре необязательных поля дают `T \| undefined` там, где ждут `T \| null` | маппер нужен по пяти полям, а не по одному; поспешный `toFixed(5)` тихо режет расход меньше 0.00001 $ - на дешёвых вызовах это потерянные центы | открыто: E0-09 (маппер `ApiUsageEntry -> insert` плюс решение, сколько знаков хранить) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/db/src/schema/system.ts` (`api_usage_log.provider`) | колонка осталась `text` без pgEnum и без рантайм-проверки; сужение E0-05 закрыло только сигнатуры репозитория | любая запись мимо репозитория кладёт произвольное имя, агрегат по правильному имени вернёт 0 | открыто: кандидат E13 (pgEnum или CHECK со своей миграцией, вместе с витриной `api_usage_daily`) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/db/src/schema/system.ts:18` (комментарий к `api_usage_log`) | список провайдеров перечислен в комментарии руками, хотя источник истины - `Provider` из `@sf/core` | комментарий разойдётся с union при первом новом провайдере | открыто: E13 (вместе со строкой про pgEnum на колонку) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/job-ids.ts` (`segment`) | запрещены только `/` и `:`; форма и длина части не проверяются - внешняя строка (id из ответа API или из ввода оператора) уходит в ключ BullMQ как есть | невалидированный внешний ввод в пространстве ключей Redis; проверка формы целиком на вызывающем | открыто: E1 (валидация `yt_channel_id`/`yt_video_id` на входе радара) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/errors.ts` (`AppError.details?: unknown`) | `details` - открытый канал наружу: `catch (e) { throw new ValidationError(msg, e) }` вокруг googleapis унесёт в него `config.url` с ключом | секрет в теле ответа api и в логе; api за basic auth, поэтому не блокер | частично закрыто E0-06 (круг правок 1): `details` отдаётся только по белому списку класса (`publicDetails` в `apps/api/src/lib/error-response.ts`: issues `ValidationError` и три поля `BudgetExceededError`, остальное - лишь в лог), `AppError` с `httpStatus` 400-599 отдаёт наружу свой статус и свой `code` (доменный код - часть контракта: `YT_UNAVAILABLE` 503 отличается от бага сервиса), а текст `message` уходит только у 400-499 и только одной строкой до 200 печатных символов; у 5xx он заменяется типовым по статусу (`docs/30_E0_TASKS.md:164`), статус вне 400-599 (3xx, 700, NaN) - 500 `INTERNAL_ERROR`. Не-`AppError` со `statusCode`: 4xx сохраняется, любой другой - 500 `INTERNAL_ERROR` (`apps/api/src/plugins/error-handler.ts`; посадка - кейсы «`AppError` с `details`, содержащим url с ключом», «5xx AppError», «impossible status», «bounds and flattens» в `apps/api/test/errors.test.ts`). Круг правок 2 закрыл и путь в лог: `logError` больше не пишет `details` как есть - в строку идёт только то, что увидел бы вызывающий (`publicDetails`), остальное заменяется меткой `[omitted]` (посадка - «keeps the secret an AppError carried in details out of the log» и «writes the details a caller would have been shown anyway» в `apps/api/test/errors.test.ts`). Остаётся открытым: (1) текст 4xx-`AppError` пишет тот, кто бросает, и `new ValidationError(err.message)` вокруг ответа провайдера унесёт его чужой текст в тело ответа - E2-04 (первый настоящий `AppError` вокруг внешнего вызова); (2) лог безопасным стоком не стал и не подаётся таковым: `logError` отдаёт pino всю ошибку, а сериализатор `pino-std-serializers` копирует все перечислимые свойства (`lib/err.js`: `for (const key in err)`), не только `message` и `stack`; `loggableError` подменяет лишь `AppError.details`, поэтому сырой отказ http-клиента, проброшенный из E1/E2 без обёртки в `AppError`, кладёт в строку и `config.url` с ключом, и `response.data` целиком - E0-11 (сбор и хранение логов на VPS) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/job-ids.ts` (`jobIds.radarScore`) | id без сегмента версии или времени - единственный способ поставить пересчёт | оператор меняет `radar.weights` и просит пересчёт: id уже в completed, BullMQ молча возвращает старый job, новой строки `trend_signal` нет | открыто: E1-06 (формат id задаёт документ E1) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/enums.ts` | файл-корзина: `SnapshotPoint` (Radar) и `AnalysisKind` (Intelligence) в одном модуле «enums», тогда как соседи названы по понятию (`idea.ts`, `provider.ts`); своего юнит-теста в core у файла нет - единственный сторож `packages/db/test/enum-parity.test.ts` | следующие перечисления (`ScriptStatus`, `PublishStatus`) лягут туда же по названию | открыто: E1/E2 (разложить по понятиям при первом новом перечислении) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/integrations/gemini/src/video-analysis.ts:15` | `pass: "full" \| "hook_pass"` - вторая, усечённая копия `ANALYSIS_KINDS` | третий вид анализа появится в core и не появится в интеграции | открыто: E2-01 (импорт `AnalysisKind` из `@sf/core`) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/integrations/youtube/src/analytics-api.ts:22` | `AnalyticsApiClient` без `UsageLogger` в конструкторе, в отличие от `DataApiClient` | вызовы Analytics не попадают в `api_usage_log` - расход виден только по одному из двух API | открыто: E7-01 (решение 7 эпика E7) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/db/test/api-usage-log.int.test.ts:160,195` | пробные провайдеры переименованы в настоящие `cartesia`/`openai_tts` ради типа `Provider` | первая фикстура E9/E10 с этими провайдерами тихо изменит смысл двух проверок изоляции окон | открыто: E9/E10 (своя фикстура вместо чужого провайдера) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/test/content-dna.test.ts` (сверка набора полей) | тест требует точного равенства набора полей, тогда как blueprint §5.2 пишет «at minimum» | любое законное расширение DNA в E2 обязано править тест - соблазн ослабить проверку | открыто: E2 (сверка по включению плюс явный список обязательных полей) |
| 07.09.2026 | E0-05, ревью круг 1 | промпты генерации (E4/E5), `ContentDnaSchema` | запрет на попадание `semanticTranscript` / `timeline[].narration` / `overlayText` конкурента в промпты генерации держится только соглашением | hard rule «анализируем, не клонируем» нарушается одной строкой сборки промпта, и никто не краснеет | открыто: E4/E5 (тип «безопасного среза DNA» плюс тест на состав промпта) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/budget.ts` (`BUDGET_WARN_RATIO`) | капы приходят из `@sf/config`, порог предупреждения зашит константой | оператор не может подвинуть порог, не пересобрав пакет | открыто: E0-09 (порог рядом с капами - env или `app_setting`) |
| 07.09.2026 | E0-05, ревью круг 1 | `packages/core/src/domain/usage.ts` (`ApiUsageEntrySchema`) | схема нигде не исполняется в рантайме - на границе интеграций работает только тип | запись, собранная из `unknown` (ответ провайдера), обойдёт правило «измерение по провайдеру: `units` для YouTube, `costUsd` для платных» | открыто: E0-09 (`safeParse` на входе `createUsageLogger`) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/db/src/schema/system.ts:14-28` (`api_usage_log`) | нет колонки исхода вызова: провал с неизвестной стоимостью обязан нести измерение и пишется как `units: 0` - неотличим от бесплатного успешного вызова | L-006 требует записи и при ошибке, но по журналу нельзя отличить провал от дешёвого успеха: разбор инцидента и витрина E13 считают их одним | открыто: E0-09 (колонка `status`/`error` вместе с первым настоящим логгером) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/core/src/domain/job-ids.ts` (`intervalSlot`) | тик repeatable-job, сработавший на несколько миллисекунд раньше границы, даёт слот предыдущего интервала, id которого ещё жив в Redis | синк этого интервала молча не ставится - пропуск без единой ошибки в логе | открыто: E1-03 (округление момента к расписанию или сдвиг слота) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/core/src/domain/budget.ts:13-18` (`BudgetInput`) | два безымянных числа: `budgetState({ spent: unitsToday, cap: geminiDailyUsd })` компилируется | units и доллары можно перепутать местами - кап проверяется не тот, вызов уходит | открыто: E0-09 (различать в `BudgetGuard` - брендированный тип или отдельные функции) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/integrations/youtube/src/index.ts:3` | `export * from "./quota.js"` остался ради одного `QUOTA_COST` после переезда `UsageLogger` в core | звёздочный реэкспорт вернёт наружу всё, что позже появится в `quota.ts` | открыто: E1-01 (именованный экспорт `QUOTA_COST`) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/integrations/tts/src/provider.ts:22` (`TtsProvider.name`) | `name: string` - третий свободный «provider» рядом с `Provider` и колонкой `api_usage_log.provider` | когда E10 начнёт писать расход, `name` разойдётся с `Provider` и строки TTS не попадут в дневной кап | открыто: E10 (`name` как подмножество `Provider`) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/core/src/domain/usage.ts` (`ApiUsageEntrySchema`) | схема - `z.discriminatedUnion`, ветки `quotaEntry`/`paidEntry`/`systemEntry` не экспортируются: `.pick()`/`.extend()` недоступны, и entry с провайдером из переменной (`provider: Provider`) без сужения не собрать | витрине E13 и контрактам E0-07 придётся описывать производную схему заново; обёртке «залогировать провал любого провайдера» в `defineJob`/`BudgetGuard` останется `switch` по ветке или каст | открыто: E0-07 (экспорт веток или общий объект полей, когда появится первый потребитель), E0-08/E0-09 (обёртка логирования) |
| 07.09.2026 | E0-05, ревью круг 2 | `apps/api` (`/health`), решение 2 `docs/30_E0_TASKS.md` § E0-06 | `/health` вне basic auth отдаёт версию, коммит и uptime | наружу уходит отпечаток сборки; должно отдаваться только `ok`/`degraded` (SEC4) | закрыто: E0-06 (публичный `/health` отдаёт только `{status}` и 200/503, версия, коммит, uptime и разбивка по зависимостям - в `/system/status` под паролем) |
| 07.09.2026 | E0-05, ревью круг 2 | `packages/integrations/youtube/src/data-api.ts` (`UsageLogger`) | после переезда типа в core `provider` в интеграции больше не сужен до `youtube_data`/`youtube_analytics` - второе ограничение старого локального типа не восстановлено | в E7-01 `DataApiClient` сможет записать расход как `youtube_analytics`, и `sumUnitsToday(db, "youtube_data")` недосчитает units | открыто: E7-01 (сузить провайдера на стороне клиента - параметр конструктора или обёртка логгера) |
| 07.09.2026 | E0-05, проверка круга 2 | `packages/core/src/domain/usage.ts` (`quotaEntry`) | `youtube_analytics` лежит в одной группе «кап в units» с `youtube_data`, тогда как у Analytics API квота своя, отдельная от 10 000 units Data API; ни `QUOTA_COST`, ни кап под `youtube_analytics` не заведены | расход двух API складывается в один дневной кап: либо Data API режется чужими units, либо Analytics уходит без предохранителя | открыто: E7-01 |
| 07.09.2026 | E0-05, проверка круга 2 | `packages/core/src/domain/usage.ts` (`paidEntry`), E2 решение 1 | `costUsd: 0` остаётся легальным значением, когда цена вызова неизвестна (нет `app_setting.gemini.prices[model]`) | `sumCostUsdToday("gemini")` снова показывает 0 при настоящем расходе, и дневной бюджет не срабатывает | открыто: E2-04/E0-09 (цена новой модели и поведение логгера при неизвестной цене) |
| 07.09.2026 | E0-05, проверка круга 2 | `packages/core/src/domain/job-ids.ts:31-39` (`segment`) | числовая часть уходит в id через `String(value)`: `jobId("x", 1e21)` даёт `x/1e+21`, дробное - `x/1.5` | формат id зависит от представления числа: два вызова с одним смыслом дают разные id, идемпотентность теряется молча | открыто: E0-09 |
| 07.09.2026 | E0-05, проверка круга 2 | `docs/37_E7_TASKS.md:88` (`analytics.ingest/<date>`), `docs/41_E11_TASKS.md:72` (`publish/<itemId>`) | id без сегмента времени или версии - как у `radarScore`, но своей строки долга у этих двух форматов не было | ручной повтор (перезабрать день аналитики, перезалить видео) BullMQ молча игнорирует: job с тем же id уже в completed | открыто: E7-01/E11 (формат id задают документы своих эпиков) |
| 07.09.2026 | E0-05, проверка круга 2 | `docs/31_E1_TASKS.md:95` | плейсхолдер `<floor(now / interval)>` сам содержит `/`; после смены разделителя на `/` читается как два сегмента id | исполнитель E1-03 строит `radar.sync/<channelId>/<floor(now>/<interval)>` по букве документа | открыто: E1-03 (заменить плейсхолдер на `<slot>`) |
| 07.09.2026 | E0-05, проверка круга 2 | `packages/core/test/usage.test.ts:80` («prices every provider») | обратный дрейф (значение ветки мимо `PROVIDERS`) закрыт кругом 3 только тип-утверждением `expectTypeOf<ApiUsageEntry["provider"]>()`; рантайм-сверки состава веток с `PROVIDERS` нет | ветка, разошедшаяся с `PROVIDERS` в рантайме (например после правки схемы без пересборки типов в CI), проходит `parse` | открыто: E0-09 (рантайм-канарейка вместе с `safeParse` в `createUsageLogger`) |
| 07.09.2026 | E0-05, проверка круга 2 | `packages/core/src/domain/budget.ts:44` (`budgetState`) | верхней границы входа нет: функция принимает любой конечный положительный `cap`, включая порядки (`> 4.5e307`), на которых её собственная арифметика произведений теряет смысл | из env такой кап прийти не может (`positive()` в долларах и units), но вызывающий с числом из другого источника получит `warn`, не соответствующий доле | открыто: E0-09 (верхняя граница входа вместе с `BudgetGuard`) |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/lib/health.ts` (`Promise.race`) | дедлайн отвечает за пробу, но саму пробу не отменяет: запрос к Postgres или Redis продолжает жить после того, как `/health` уже ответил | при лежащей зависимости каждый вызов healthcheck оставляет за собой висящий запрос; на частом healthcheck Compose это растущий хвост | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `packages/db/src/client.ts` (postgres-js) | у клиента не задан `connect_timeout`: сколько ждать недоступный Postgres, решает драйвер, а границу ставит только дедлайн пробы api | `pingDb` и любой запрос вне `/health` (миграции, worker E0-08) висят на подключении без своей границы | закрыто: E0-08 (`connect_timeout` 10 с в `createDb`, переопределяется параметром; посадка `packages/db/test/client.test.ts` читает опцию с самого клиента) |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/plugins/openapi.ts`, `apps/api/src/routes/**` | конверт ошибки (`{error:{code,message,requestId,details?}}`) не описан ни одной схемой ответа: `components.schemas` в `/openapi.json` пуст, а `setErrorHandler` шлёт тело мимо схем роутов | первый роут с `response: { 404: … }` (E3 «идея не найдена») заставит сериализатор zod разбирать конверт чужой схемой - 404 придёт клиенту как 500; клиент E0-07 типизирует ошибки руками | закрыто: E0-07, круг правок 1 (`ErrorBodySchema` в `@sf/contracts`, `errorResponses()` с ключами `4xx`/`5xx` в обоих роутах, `transformObject: createJsonSchemaTransformObject({ ErrorBody })` в `registerOpenapi`; точный `503` у `/health` объявлен `withErrorEnvelope(HealthResponseSchema)` - иначе ошибка с этим статусом сериализовалась схемой здоровья и уходила как `FST_ERR_FAILED_ERROR_SERIALIZATION` с 500; посадка - `apps/api/test/openapi.test.ts` (`components.schemas` не пуст, `4XX`/`5XX` ссылаются на `#/components/schemas/ErrorBody`) и `test/errors.test.ts` - «answers /health with the envelope, not with the health shape») |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/test/health.test.ts`, `test/health.int.test.ts`, `test/helpers.ts` | `HealthBodySchema` скопирована в два тестовых файла, `ErrorBody` в `helpers.ts` - третья копия формы ответа | смена контракта правится в трёх местах; разъехавшаяся копия делает тест зелёным на неверном ответе | закрыто: E0-07 (`HealthResponseSchema` и `ErrorBodySchema` берутся из `@sf/contracts` - в `test/helpers.ts`, `test/health.test.ts` и `test/health.int.test.ts` копий формы не осталось) |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/lib/shutdown.ts` | db и redis закрываются последовательно, хотя друг от друга не зависят | зависшее закрытие БД съедает дедлайн, отведённый обоим: Redis не закрывается вовсе | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/plugins/auth.ts` (`PUBLIC_ROUTES`) | публичность роута объявлена списком литералов в плагине, а не рядом с самим роутом | E9/E11 заводят публичную отдачу медиа и правят чужой файл; опечатка в пути даёт молча закрытый роут вместо ошибки | открыто: E9/E11 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/plugins/auth.ts`, `plugins/openapi.ts`, `src/lib/logger.ts` | тесты дублируют литералы `/docs`, `/openapi.json` и путей `redact` вместо того, чтобы брать `DOCS_ROUTE`, `OPENAPI_ROUTE` и `REDACTED_LOG_PATHS` из кода (`REQUEST_ID_HEADER` они как раз импортируют); сами константы потребителей имеют - `plugins/openapi.ts:56,60` и `lib/logger.ts:42` | перенос `/docs` пройдёт typecheck и покрасит тесты 404-ом, который прочитают как регресс auth | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/server.ts:12-23` против `apps/api/src/routes/index.ts` | карта модулей по эпикам живёт докблоком точки входа, а настоящий реестр роутов - в другом файле | модуль добавят в реестр и забудут в карте: докблок становится ложным путеводителем по api | открыто: E0-09 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/app.ts` (`buildApp(deps, options)`) против `docs/30_E0_TASKS.md:161` | документ обещает `buildApp(deps)`, у функции второй обязательный параметр (пароль и опции логгера) | автор E0-09 пишет вызов по документу и не собирает инстанс | открыто: E0-09 (вместе с полями `db`/`queues` в `AppDeps`) |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/lib/redis.ts` (`onError`), `apps/api/src/server.ts:47-49` | уровень `error` на каждой попытке переподключения ioredis | лежащий Redis даёт поток одинаковых строк уровня `error`, в котором тонет всё остальное | сужено: воркер закрыт E0-08 (`createRedisErrorReporter`: первая ошибка выходит `error`, следующие - `warn`, с шестой - `debug` со счётчиком, сброс на `ready`); в `apps/api` уровень прежний - открыто, чинится вместе с выносом фабрики в общий пакет |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/routes/health/index.ts` | у публичного `/health` нет ни `Cache-Control`, ни своего `bodyLimit` | единственный роут без пароля на каждый запрос ходит в Postgres и Redis - дешёвый способ нагрузить обе зависимости | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 1 (оставлено) | `apps/api/src/lib/` | каталог собрал логгер, redis, health, shutdown и форму ошибки - всё, что не плагин и не роут | следующий модуль ляжет туда же по названию каталога; границы между инфраструктурой и доменом api не видно | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/package.json` | `@sf/youtube` и `bullmq` в зависимостях без единого импортёра (потребитель - E0-09/E1); `bullmq` при этом - единственный пакет в дереве api, читающий `process.env.NODE_ENV` | лишний рантайм в образе E0-11 и в дереве проверки строки :43 | открыто: E0-09/E1 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/vitest.int.config.ts` (к строке :24) | конфигов стало шесть unit и два int; int-конфиг api повторяет `packages/db/vitest.int.config.ts` строка в строку | правка тестовой политики - в восемь мест вместо одного | открыто: E0-12 (вместе с :24) |
| 08.09.2026 | E0-06, ревью круг 2 (техдолг рядом) | `apps/api/vitest.int.config.ts` | нет `fileParallelism: false`, при этом turbo гонит `@sf/db:test:int` (там `DROP SCHEMA public CASCADE`) и `@sf/api:test:int` по одной базе параллельно | сегодня безопасно - api-int только пингует; первый int-тест api, который читает таблицу, станет флапающим без всякой связи с кодом | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/src/lib/redis.ts`, `apps/worker` (E0-08) | `createRedis` получит второй экземпляр в worker: там нужны другие опции ioredis (`maxRetriesPerRequest: null` для BullMQ) | осознанный дубль на двоих; третий потребитель размножит его дальше | принято: E0-08 завёл вторую копию осознанно (`apps/worker/src/lib/redis.ts`, `lib/shutdown.ts`, `lib/logger.ts` - опции противоположные, импорт между apps запрещён §6); цена - правка в двух местах. Переоткрыть при третьем потребителе: тогда `createRedis`/`shutdown`/`logger` выносятся в пакет |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/src/server.ts:64` (`host: "0.0.0.0"`) | api слушает все интерфейсы | в E0-11 порт api не должен публиковаться наружу Compose - иначе единственный пароль сервиса становится единственной защитой из интернета | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 4 | `apps/api/src/app.ts` (опция `clientErrorHandler` не задана) | отказы уровня сокета (`defaultClientErrorHandler` Fastify: 400, 408, 431) идут мимо `frameworkErrorHandler` и мимо нашего конверта, без `requestId`, строкой уровня `trace` | клиент E0-07 упадёт на 431 (крупная cookie) или 408 вместо «ошибка сервиса»; поток кривых HTTP-запросов на опубликованном порту E0-11 не оставляет ни одной строки | открыто: E0-11 (вместе с `host: "0.0.0.0"` выше) |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/src/plugins/auth.ts` | basic auth без защиты от CSRF | пока все роуты `GET` - безвредно; первый `POST` (E1+), вызванный из браузера оператора, потребует решения | открыто: E1 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/test/logger.test.ts` | `REDACTED_LOG_PATHS` посажен на состав конфига, а не на поведение pino | правило, переставшее скрывать заголовок (другая форма пути, другой логгер), останется зелёным, а `authorization` уйдёт в лог | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/test/health.int.test.ts:55,76` | проба собирает `errors` и возвращает их, но ни один кейс их не проверяет | причина отказа пробы может стать пустой или чужой - никто не заметит | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 1 (техдолг рядом) | `apps/api/src/plugins/auth.ts` (`secretsMatch`), `apps/api/src/lib/error-response.ts` (`VALIDATION_ERROR_CODE`) | пароль пере-хешируется sha256 на каждый запрос; код валидации продублирован рядом с `ValidationError` из `@sf/core` | лишний хеш на каждом запросе; два источника одного кода расходятся молча | частично закрыто: E0-07 (`VALIDATION_ERROR_CODE` объявлен в `@sf/core` рядом с `ValidationError`, `@sf/contracts` его реэкспортирует, в api литералов кодов нет); пере-хеширование пароля sha256 на каждый запрос - открыто: E13-07 (адрес исправлен в круге правок 1 E0-07: E0-09 - учёт стоимости и `BudgetGuard`, `plugins/auth.ts` он не трогает; basic auth правит E13-07 «Защита доступа») |
| 08.09.2026 | E0-06, ревью круг 2 (техдолг рядом) | `apps/api/src/server.ts:36-45` | отображение `env.APP_VERSION`/`GIT_COMMIT` в `deps.buildInfo` живёт только в точке входа и закрыто лишь ручным смоуком | перепутанные местами версия и коммит или потерянный `?? null` обнаружатся только глазами в `/system/status` | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 2 (техдолг рядом) | `apps/api/src/plugins/openapi.ts:56` | `@fastify/swagger-ui` зарегистрирован без `staticCSP`/`transformStaticCSP` - `/docs` отдаётся без CSP | страница документации без политики контента; адрес находки - E13-07 | открыто: E13-07 |
| 08.09.2026 | E0-06, ревью круг 2 (техдолг рядом) | `apps/api/src/server.ts:29` | `logRedisError` до `buildApp` - no-op: ошибка ioredis в этом окне пропадает | окно синхронное и практически недостижимое; цена - потерянная строка лога при отказе Redis на старте | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 2 (придирка) | `apps/api/src/lib/redis.ts:75-81`, `apps/api/src/lib/shutdown.ts:88-93` | `closeRedis` глушит любой отказ `quit()` и молча делает `disconnect()`: «всегда отказ» заменено на «никогда не отказ», третьего исхода нет, лог в обоих случаях `closed target=redis` | Redis отвечает на QUIT ошибкой (ACL, прокси, протокол) - выход 0 и «закрыто», оператор не узнает, что клиент снесён сокетом | открыто: E0-11 |
| 08.09.2026 | E0-06, ревью круг 2 (придирка) | `apps/api/src/deps.ts:10-43` | контрактные схемы (`ProbeStatusSchema`, `HealthReportSchema`, `BuildInfoSchema`) лежат в модуле внедрения зависимостей, а схема ответа `/health` осталась инлайном в роуте | E0-07 соберёт контракты из файла про проводку зависимостей и не найдёт половину | закрыто: E0-07 (`ProbeStatusSchema`, `HealthReportSchema`, `BuildInfoSchema` - в `packages/contracts/src/system.ts`, ответ `/health` - в `src/health.ts`; `deps.ts` держит только `HealthProbes` и `AppDeps`) |
| 08.09.2026 | E0-06, ревью круг 2 (придирка) | `apps/api/test/shutdown.test.ts:76-116`, `apps/api/test/redis.test.ts:40-48` | дедлайны посажены на реальных таймерах (20/40/60 мс) без `vi.useFakeTimers`; фолбэк `closeRedis` проверен только ручным стабом с текстом ошибки ioredis, живым Redis не проверялся | тест может мигать на загруженном раннере; смена поведения `quit()` в новой версии ioredis обнаружится только в проде | открыто: E0-12 |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:234-239` | у 5xx-`AppError` наружу уходит `code`, и он, в отличие от `message`, не проходит `safeMessage` - ни границы длины, ни проверки печатности | `new AppError(String(err.code ?? err.message), ..., 502)` в E2/E7 отдаст клиенту многострочный или километровый `code` в теле и в логе | частично закрыто: E0-07, круг правок 1 - тело (`safeCode` в `plugins/error-handler.ts`: наружу уходит только `code` формы `UPPER_SNAKE` не длиннее 64 символов, всё прочее заменяется - на `INTERNAL_ERROR` у 5xx и на код статуса из `CLIENT_ERROR_CODES` у 4xx, а не обрезается; проверка именно по форме, а не по печатности и пробелам: url провайдера с ключом - один печатный токен без пробелов и такую проверку проходит (круг правок 2); посадка - кейсы `refuses to send a 5xx code that is ...`, `refuses a 5xx code carrying a separator ...`, `refuses to send a code that is a url with a key in it (5xx|4xx)` и `names a 4xx with an unusable code ...` в `test/errors.test.ts`); строка лога - открыто: E0-11 (`loggableError` пишет исходный `err.code` намеренно - лог берёт больше ответа, - но своей границы длины у него нет; решается вместе с прочей защитой вывода и сбором логов на VPS) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:257-259` | ветка `isFastifyError` повторяет клиенту `message` ошибки `@fastify/error`, а эти сообщения - шаблоны с подстановкой ввода (`'%s' is not a valid url component`), то есть ввод возвращается в теле; `setNotFoundHandler` и обработчик ошибок роутера в том же файле ввод как раз не эхают | принцип «не эхаем ввод» держится в одной из трёх веток одного файла; следующий автор скопирует любую | закрыто: E0-07 (решение техлида Р3 от 09.09.2026: любой не-`AppError` 4xx отвечает типовым текстом по статусу, `isFastifyError` и `readMessage` удалены; кейс 418 в `test/errors.test.ts` требует, чтобы ввода в теле не было) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:270-296` (докблок `loggableError`) | докблок обещает, что лог берёт больше ответа - stack, cause, исходный текст 5xx, - но при `AppError` с `details` собирается плоский объект из шести полей и `cause` теряется | одна и та же ошибка логируется по-разному в зависимости от того, заполнил ли бросающий `details`: с `details` цепочка причин исчезает ровно при разборе инцидента | открыто: E0-11 (вместе со сбором логов на VPS) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:249-262` | у не-`AppError` со `statusCode` 5xx (503 будущего `under-pressure`) статус схлопывается в 500, хотя для `AppError` тот же диапазон сохраняется; асимметрия нигде не записана | плагин, отвечающий 503 при перегрузке, будет виден клиенту и балансировщику как 500 - ретрай вместо разгрузки | открыто: E0-11 (вместе с плагинами защиты от перегрузки) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:98-105` (`statusMessage`) | 5xx вне таблицы (507, 599) получает текст `Internal Server Error` при своём, чужом статусе; 414 (`FST_ERR_MAX_PARAM_LENGTH`) тоже не в `CLIENT_ERROR_CODES`/`STATUS_MESSAGES` и получает `code: "CLIENT_ERROR"` вместо своего кода | тело описывает не тот отказ, что код ответа: читатель лога и клиент расходятся в диагнозе; клиент E0-07 и web не отличат «URI слишком длинный» от любого другого 4xx вне таблицы | открыто: E0-11 (вместе с асимметрией 5xx выше) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/src/plugins/error-handler.ts:129-132` (`isPrintable`) | пропускает C1-управляющие (0x80-0x9F, включая CSI 0x9B), которые JSON не экранирует, хотя докблок обещает, что escape-последовательность не дойдёт до терминала | текст 4xx-`AppError`, собранный из внешнего ответа, донесёт управляющую последовательность до терминала оператора, читающего тело curl-ом | закрыто: E0-07, круг правок 1 (`isPrintable` проверяет классы `\p{C}`/`\p{Z}` вместо диапазона: C1-управляющие, `U+0085`, `U+2028`/`U+2029` и `U+00A0` не проходят ни в `code`, ни в `message`; посадка - `apps/api/test/errors.test.ts`, кейсы «refuses a 5xx code carrying a separator a range check would miss» и «flattens the separators of a message a range check would miss») |
| 08.09.2026 | E0-06, ревью круг 3 (круг 4: расширено) | `apps/api/src/plugins/error-handler.ts:298-313` (`logError`), `apps/api/src/plugins/auth.ts`, `frameworkErrorHandler` | `warn` с полным `err` пишется не только на 401 от `auth.ts`, но и на каждый кривой URL, который `frameworkErrorHandler` отклоняет до аутентификации | аноним на опубликованном порту накачивает лог одинаковыми строками ещё до пароля, настоящие отказы в них тонут | открыто: E0-11 (вместе с `host: "0.0.0.0"` и сбором логов; уровень и состав до-авторизационного лога выбираются одним решением с молчащим `setNotFoundHandler` ниже) |
| 08.09.2026 | E0-06, ревью круг 3 | `packages/config/src/schema.ts:44-50` (`buildLabel`) | значение, не похожее на метку, молча становится `undefined`: ни строки в лог, ни отдельного исхода | `APP_VERSION="1.4.0 (2026-09-08)"` в E0-11 даёт `"version": null` - неотличимо от «переменную забыли передать» | открыто: E0-11 (вместе с build-args образа) |
| 08.09.2026 | E0-06, ревью круг 3 | `turbo.json` (`globalEnv`/`env`), `packages/config/src/schema.ts:89-90` | `APP_VERSION` и `GIT_COMMIT` не объявлены в `turbo.json`, а turbo прогоняет задачи в strict env | значения из шелла до сервиса под turbo не доходят: `/system/status` покажет `null` там, где сборка их передала | открыто: E0-12 (вместе с прочими правками конвейера) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/test/openapi.test.ts:74` | `/system/status` в документе проверяется только на присутствие пути (`toBeDefined()`), без сверки формы ответа | схема роута может выпасть из `jsonSchemaTransform` при переносе - тест останется зелёным | открыто: E0-12 (вместе с общим пресетом vitest) |
| 08.09.2026 | E0-06, ревью круг 3 | `packages/config/test/load-env.test.ts` | нет кейса на пустую строку `APP_VERSION=""`/`GIT_COMMIT=""` - нижняя граница длины метки не посажена | регресс правила `BUILD_LABEL` на пустом значении не поймается | открыто: E0-12 (вместе с прочей дисциплиной тестов) |
| 08.09.2026 | E0-06, ревью круг 3 | `apps/api/test/errors.test.ts:440-445` | `toContain("gemini")` выполняется и от текста сообщения, а не только от записанных `details`; кейс держится на парном `not.toContain("[omitted]")` | подмена `publicDetails` на пустую выдачу оставит кейс зелёным наполовину | открыто: E0-12 (вместе с прочей дисциплиной тестов) |
| 08.09.2026 | E0-06, ревью круг 4 | `apps/api/src/plugins/error-handler.ts:352-371` (`frameworkErrorHandler`) против `apps/api/src/app.ts:111-114` (`onSend`) | ответ на отказ роутера идёт мимо `onSend` (по докблоку - осознанно) и мимо `onResponse`: в логе `incoming request` + `request rejected`, ни одной `request completed` | E13-07 поставит CSP и security-заголовки одним `onSend`-хуком - эти ответы останутся без них; счётчик запрос/ответ и latency в E13 недосчитает router-отказы | открыто: E13-07 (CSP), E13 (счётчики и latency) |
| 08.09.2026 | E0-06, ревью круг 4 | `apps/api/src/plugins/error-handler.ts:393-405` (`setNotFoundHandler`) | из трёх обработчиков одного конверта два зовут `logError` (`registerErrorHandler`, `frameworkErrorHandler`), `setNotFoundHandler` не пишет ничего | перебор путей аутентифицированным клиентом не даёт ни одной строки лога на 404 | открыто: E0-11 (одним решением со строкой про до-авторизационный `warn` выше - уровень и состав до-авторизационного лога выбираются разом) |
| 08.09.2026 | E0-06, ревью круг 4 | `apps/api/src/lib/error-response.ts:4-10` (`REQUEST_ID_HEADER`) | HTTP-заголовок живёт в модуле формы тела ошибки, `app.ts` держит реэкспорт ради импортёров | следующий заголовок положат туда же «по аналогии» | закрыто: E0-07 (`REQUEST_ID_HEADER` объявлен в `packages/contracts/src/errors.ts` - один источник для api и `@sf/api-client`; реэкспорт из `app.ts` убран, импортёры берут константу из пакета) |
| 09.09.2026 | E0-07, ревью круги 1-2 | `apps/web/next.config.ts:7` | `transpilePackages: ["@sf/config"]` - два новых source-only пакета (`@sf/contracts`, `@sf/api-client`) туда не добавлены; web их пока не импортирует, поэтому `pnpm --filter @sf/web build` провалиться на них не может | первый импорт в E0-10 падает на резолве исходников; «пароль не в клиентском чанке» до этого держится резолвом условий и маркером `server-only`, а не прогоном сборки | открыто: E0-10 (условие записано в `docs/adr/0001-config-server-entry.md` и в `docs/30_E0_TASKS.md` § E0-10) |
| 09.09.2026 | E0-07, ревью круги 1-2 | `packages/api-client/test/client.test.ts`, `packages/api-client/test/exports.test.ts` | у клиента нет ни одного кейса против настоящего приложения (`app.inject`) - только мок-`fetch`; замок двух входов проверяется чтением манифеста и текста модуля, а не прогоном бандлера | расхождение клиента и api ловят только общие схемы `@sf/contracts`; замок `browser`-условия по-настоящему проверяется только сборкой web | открыто: E0-10 (там появляется живой потребитель и сборка) |
| 09.09.2026 | E0-07, ревью круг 1 (придирка A7) | `apps/api/src/lib/route-schemas.ts` (`errorResponses()`) | функция без параметров, возвращающая один и тот же литерал: параметры исчезли вместе с вариантом B при решении Р1 (`DECISIONS.md`, 09.09.2026), константа со спредом читалась бы честнее; намеренно не менялось в E0-07 - имя и форма названы решением | читатель ищет, что меняется между вызовами; при пересмотре Р1 объяснение придётся искать в файле дела E0-07 | открыто: E1-01 (первый роут вне E0 покажет, нужны ли параметры) |
| 09.09.2026 | E0-07, ревью круг 1 (придирка) | `packages/api-client/src/client.ts` (`AUTH_USERNAME`) | имя пользователя basic auth зашито литералом `admin` в клиенте, api имя не проверяет вовсе (`DECISIONS.md`, 08.09.2026) | со вторым потребителем (middleware web, E0-10) значение придётся согласовывать через две копии литерала | открыто: E13-07 «Защита доступа» (задача, которая правит `plugins/auth.ts`) |
| 09.09.2026 | E0-07, ревью круги 1-2 (придирки) | `packages/contracts/src/errors.ts:53-68` и `apps/api/src/plugins/error-handler.ts:44-59` | `CLIENT_ERROR_CODES` (контракты) и `STATUS_MESSAGES` (api) - параллельные таблицы по одним ключам 400…429 в разных пакетах; там же `INTERNAL_ERROR_MESSAGE` уехал в контракты, а `CLIENT_ERROR_MESSAGE` остался в api | добавление 414 (строка ниже) делается дважды; пропуск одной даёт код и текст из разных наборов, а контракты копят презентационные строки api | открыто: E0-11 (вместе со строкой про 5xx вне таблицы и 414) |
| 09.09.2026 | E0-07, ревью круг 1 (придирка) | `packages/contracts/src/errors.ts` (`ErrorDetailItemSchema`) | схема экспортирована, но ни в один контракт не входит и никем не парсится: `details` в конверте остался `z.unknown()` (граница решения Р1 - форма конверта не меняется) | клиент и web не узнают из контрактов форму `details` у `VALIDATION_ERROR` и опишут её второй раз | открыто: E0-10 (первый потребитель - разбор `details` на странице) |
| 09.09.2026 | E0-07, ревью круг 1 (придирка) | `packages/contracts/test/errors.test.ts:66-77` | тест контрактов проверяет инвариант `@sf/core` (`.code` класса = константа), уже прибитый в `packages/core/test/errors.test.ts` | смена кода в core красит чужой пакет, свой остаётся зелёным | открыто: E0-12 (вместе с прочей дисциплиной тестов) |
| 09.09.2026 | E0-07, ревью круг 2 | `apps/api/src/routes/README.md`, `apps/api/src/lib/route-schemas.ts` | класс дефекта из строки про конверт ошибки (роут объявляет точный статус без `withErrorEnvelope()` -> `FST_ERR_FAILED_ERROR_SERIALIZATION` и 500) закрыт абзацем README, а не проверкой: ни линт, ни тест этого не ловят | первый роут E1+ с точным `404`/`409` повторит дефект, и увидят его на проде | открыто: E0-12 (замок в `.claude/hooks/rules.sf.json` на `apps/api/src/routes/**`) |
| 09.09.2026 | E0-07, ревью круг 2 (предложение архитектуры 1) | `.claude/hooks/rules.sf.json`, `apps/api/src/routes/**` | нет замка на форму объявления роута: `Reply` обязан читаться из объявленных ответов (`SuccessReply<typeof responses>`), иначе связь схемы и хендлера снимается одной строкой | автор E1 копирует роут, пишет `Reply` руками и снова разводит схему с хендлером - typecheck молчит | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 2 (предложение архитектуры 2) | `.claude/hooks/rules.sf.json`, `packages/contracts/**`, `packages/api-client/**` | направление зависимостей новых пакетов держится только §6 `10_SYSTEM_DESIGN.md`: замка по образцу правила для `packages/core/src/**` нет | импорт `@sf/config` или `@sf/core` в клиент пройдёт ревью как «apps -> packages» | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 2 (предложение архитектуры 3) | `packages/contracts/src/health.ts` | описатель роута в контракте сделан наполовину: статусы ответа (`HEALTH_STATUS_CODES`) общие для роута и клиента, но `path` и признак публичности по-прежнему живут в api (`PUBLIC_ROUTES` в `plugins/auth.ts`) и повторяются в клиенте строкой пути | третий потребитель (middleware web, E0-10) заведёт третью копию списка публичных путей | открыто: E0-10 |
| 09.09.2026 | E0-07, ревью круг 2 | `packages/contracts/package.json` | у пакета объявлен только `main`, карты `exports` нет, а `docs/31_E1_TASKS.md:83,188` планирует подпути вида `@sf/contracts/radar/channels` | подпути придётся вводить вместе с первым модулем E1, задним числом к уже написанным импортам | открыто: E1-01 |
| 09.09.2026 | E0-07, ревью круг 2 | `apps/api/src/plugins/error-handler.ts` | файл вырос до ~440 строк и держит три обработчика, две таблицы и все границы вывода | правка любой границы требует прочитать весь файл; сюда же придут 414 и 5xx вне таблицы | открыто: E0-11 (вместе с правкой таблиц статусов) |
| 09.09.2026 | E0-07, ревью круг 2 | `docs/30_E0_TASKS.md` (решения «по умолчанию» E0), `docs/adr/` | решения эпика, реализованные в E0 (пакеты `@sf/contracts` и `@sf/api-client`, единый конверт ошибки, соглашение о кодах), в ADR не записаны - по `docs/adr/README.md` это делается при закрытии эпика | к E1 источником решения остаётся файл дела задачи, который после закрытия не читают | открыто: при закрытии E0 (оркестратор) |
| 09.09.2026 | E0-07, ревью круг 3 | `docs/adr/0001-config-server-entry.md` (абзац про `transpilePackages`), `docs/30_E0_TASKS.md` § E0-10, `docs/TECH_DEBT.md` (строка про `transpilePackages`) | ADR называет в `transpilePackages` только `@sf/api-client`, задача и техдолг - `@sf/api-client` и `@sf/contracts`, а фактический граф импорта web - `api-client -> contracts -> @sf/core`, и `@sf/core` тоже source-only и в списке ни у кого не назван | E0-10 выполняет записанное условие буквально и всё равно падает на `Module parse failed` на исходниках `@sf/core`; три документа одного диффа расходятся в списке пакетов | открыто: E0-10 (первым шагом свести список к трём пакетам и проверить сборкой) |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/api-client/src/client.ts` (ветка конверта, `:293-300`) против `:36-43,159-169` | круг 2 ограничил длину и форму только у заголовка `x-request-id`; `code`, `message` и `requestId`, взятые из тела конверта недоверенного хопа, уходят в `ApiError` без границ (`ErrorBodySchema` требует лишь `z.string().min(1)`), хотя api свои значения фильтрует `safeCode`/`safeMessage` именно поэтому | прокси отвечает конвертом с `requestId` в 8 КиБ и `U+2028`: значение уезжает в лог web и в `error.tsx` как «наш» идентификатор и рвёт строку JSON-лога надвое | открыто: E0-10 (вместе с первым живым потребителем клиента) |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/api-client/src/client.ts:36-43` против `apps/api/src/app.ts:28` | форма корреляционного id объявлена дважды и в разных пакетах, на клиенте шире: `^[!-~]{1,128}$` против `^[\w.:-]{1,128}$` у api; в `@sf/contracts` лежит только имя заголовка, формы нет - тот же класс, что закрытый блокер Б11 | ужесточение шаблона в api до клиента не доедет; значение с кавычками и `;` от промежуточного хопа проходит как «наш» id | открыто: E0-10 (форму - в `@sf/contracts` рядом с `REQUEST_ID_HEADER`) |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/contracts/src/errors.ts` (`ErrorBodySchema.details`), `packages/contracts/test/errors.test.ts` | план задачи обещал «конверт с `details: null` отвергается», но по решению Р1 `details` - `z.unknown().optional()`, а `z.unknown()` принимает `null`; докблок `apps/api/src/lib/error-response.ts:26-27` при этом называет `"details": null` состоянием, которого быть не должно | ответ с `"details": null` от сломанного хопа или бага сериализации проходит валидацию клиента как штатный - класс дрейфа, ради которого схемы и заведены | открыто: E0-10 (либо сузить `details`, либо снять обещание из докблока - решает техлид) |
| 09.09.2026 | E0-07, ревью круг 3 | `apps/api/src/lib/route-schemas.ts` (`SuccessReply`, литерал `OK_STATUS = 200`), `apps/api/src/routes/README.md` | связь схемы и типа хендлера собрана только для успеха 200; роут с успехом 201/202/204 (`docs/31_E1_TASKS.md:83,100`) ключа 200 не объявит и `SuccessReply` не соберёт, а писать `Reply` руками README запрещает | на первом же POST в E1 связь, ради которой чинили блокер Б9, снимается | открыто: E1-01 (ключ успеха - параметром или из объекта ответов) |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/api-client/src/client.ts` (докблок `basicCredentials`), `packages/api-client/test/client.test.ts:413-417` | два утверждения не подтверждаются прогоном: `btoa` на не-ascii пароле из Latin-1 не бросает, а молча даёт другие credentials (бросает только выше U+00FF), и отсоединённый `fetch` в Node не бросает `Illegal invocation`; кейса с не-ascii паролем нет | читатель упростит цикл кодирования, тесты останутся зелёными, а оператор с Latin-1 паролем получит 401 в web без подсказки | открыто: E0-10 |
| 09.09.2026 | E0-07, ревью круг 3 | `apps/api/src/routes/README.md:11`, `:33-36` | счётчик «Two rules» не поправлен после третьего пункта, добавленного правкой Б9; отдельно - порядок спреда `errorResponses()` назван тем, что удерживает точный ключ, хотя Fastify выбирает сериализатор поиском «точный статус -> `Nxx` -> `default`» (`fastify/lib/schemas.js:148-171`), а порядок ключей не читает | следующий автор считает порядок несущим и полагается на него вместо `withErrorEnvelope()` | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 3 | `docs/adr/0001-config-server-entry.md` (раздел «Откат»), `docs/adr/README.md:30` | рецепт отката расширен не был и описывает только `@sf/config`, хотя запись теперь про два пакета; указатель в индексе («последний абзац „Последствий“») ведёт не туда - абзацы про `@sf/api-client` восьмой и девятый из десяти | откат по ADR оставит `packages/api-client/src/server.ts`, условный вход и `server-only` на месте; читатель индекса попадает на рецепт отката `@sf/config` | открыто: E0-10 |
| 09.09.2026 | E0-07, ревью круг 3 | `CLAUDE.md` § Hard rules («Направление зависимостей») против `docs/10_SYSTEM_DESIGN.md` §6 | §6 диффом расширен рёбрами `contracts -> core` и `api-client -> contracts`, а сводка правил в `CLAUDE.md` их не знает | worker и ревьюер, читающие только `CLAUDE.md`, не имеют записанного правила для двух новых пакетов; импорт `@sf/config` в клиент пройдёт как «apps -> packages» | открыто: E0-12 (вместе с замком на направление) |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/contracts/test/`, `packages/api-client/test/exports.test.ts` | `HEALTH_STATUS_CODES`/`HEALTH_ANSWER_STATUSES` живут в `@sf/contracts`, а единственный замок на них - в `apps/api/test/openapi.test.ts`: пакет, владеющий фактом, его не проверяет; реэкспорт в `exports.test.ts` держится `toContain` по тексту модуля, а не по набору экспортов | смена факта в контрактах красит чужой пакет, свой остаётся зелёным; добавленный экспорт `client.ts` не попадёт во вход `./server` незамеченным | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 3 | `docs/TECH_DEBT.md` (строка про копии `vitest.config.ts`) | число исправлено на семь, но `packages/config/vitest.config.ts` в списке не учтён - копий восемь | E0-12 заведёт общий пресет и пропустит один пакет | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 3 | `apps/api/test/errors.test.ts:57` | один экземпляр объекта `failing = { schema: { response: errorResponses() } }` передан пятнадцати роутам - общий изменяемый объект в конфигурации роутов | первый же роут с `querystring` скопирует приём и получит общее состояние между роутами | открыто: E0-12 |
| 09.09.2026 | E0-07, ревью круг 3 | `packages/contracts/src/health.ts` (`HEALTH_ANSWER_STATUSES`) | объявлен как `readonly number[]`, литеральные типы значений теряются | потребитель, который захочет `switch` по статусу, получит `number` и недостижимую ветку `default` | открыто: E0-10 |
| 09.09.2026 | E0-07, ревью круг 3 | `apps/api/src/lib/error-response.ts` (`toDetailItems`), `apps/api/src/plugins/error-handler.ts` (`safeMessage`) | `issue.message` из `ValidationError` переносится в `details` дословно (прогон: ключ провайдера в тексте issue уехал в тело), и текст 4xx-`AppError` уходит без фильтра формы; `TECH_DEBT` называет путь через `message` 4xx (адрес E2-04), путь через текст issue - нет, а `details` теперь объявлен в контракте и клиент кладёт его в `ApiError.details` для web | ключ провайдера из текста ошибки валидации уезжает в тело ответа и на страницу web | открыто: E2-04 (вместе со строкой про текст 4xx) |
| 09.09.2026 | E0-07, ревью круг 3 | `apps/api/src/plugins/error-handler.ts` (`CODE_SHAPE`) | фильтр по форме идентификатора пропускает секрет, целиком состоящий из заглавных, цифр и `_` (прогон: `SK_LIVE_51H8ZQ2EJKLMNOPQRSTUVWX` уходит в тело); для ключей Google/Gemini форма вход закрывает, поэтому блокер Б7 закрыт, но докблок звучит шире гарантии | следующий автор прочитает докблок как «секрет не пройдёт» и перестанет проверять источник `code` | открыто: E0-11 (вместе со строкой про `code` в логе) |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/src/runtime.ts` (старт) | синк планировщиков и применение переключателей открывают `Queue` по всему реестру (29 объектов на одном соединении), чтобы найти чужие Job Schedulers и снять паузу | при росте реестра до ~50 очередей старт делает столько же лишних обходов Redis; сейчас это десятки миллисекунд | принято: цена мала, альтернатива - хранить список очередей со схедулерами отдельно и разойтись с реестром. Переоткрыть, если старт воркера станет заметно медленным или реестр перевалит за 50 имён |
| 10.09.2026 | E0-08, ревью круг 1 | `apps/worker/src/lib/dlq.ts:28-38` (`DlqRecord`), `apps/worker/test/dlq.int.test.ts` | форма записи `system.dlq` - контракт писателя (worker) и читателя (api E13-02) - объявлена рукописным TS-интерфейсом внутри `apps/worker`, без Zod и без валидации на чтении; копия формы уже лежит в int-тесте, тогда как `QueueSwitchesSchema` по той же причине вынесена в `@sf/core` | E13-02 заведёт третью копию; поле, добавленное в `toDlqRecord`, пройдёт мимо читателя молча | открыто: вопрос техлиду (Zod-схема записи в `@sf/core`, `parse` перед `add` и на чтении) - отдельная мелкая задача перед E13-02 |
| 10.09.2026 | E0-08, ревью круг 1 | `apps/worker/src/lib/dlq.ts:146` (`removeOnFail: false`), `docs/43_E13_TASKS.md:32` | `system.dlq` пишется без ретеншна и без обработчика: записи вечно лежат в `waiting` с полным `data` и `stacktrace`, счётчика и порога алерта нет | падающий по расписанию джоб даёт сотни тысяч записей в год; Redis упирается в память - встают все очереди сразу | открыто: E13-02 (ретеншн, порог алерта по размеру DLQ, discard-all) |
| 10.09.2026 | E0-08, ревью круг 1 | `packages/core/src/domain/redis-keys.ts:18` (`WORKER_HEARTBEAT_TTL_SEC`) | TTL - параметр рантайма воркера, а не доменная константа `@sf/core`; связь с частотой тика держится тестом (`apps/worker/test/system-heartbeat.test.ts`), но не типом и не общим источником | частоту heartbeat меняют в воркере, TTL остаётся в core: ключ протухает между тиками, healthcheck E0-11 перезапускает живой воркер | открыто: E0-09/E0-11, вместе с читателем ключа |
| 10.09.2026 | E0-08, ревью круг 1 | `apps/worker/test/schedules.test.ts:102` | пагинация синка проверяется `toHaveLength(150)`, а не точным списком id; соседний кейс назван «declares every schedule against a queue of the registry», а проверяет только регекс cron-паттерна | пагинация задвоила и потеряла по одному id - счётчик тот же, тест зелёный; расписание на очередь без обработчика пропустит именно тот кейс, который по имени за это отвечает | открыто: следующая задача, трогающая `schedules.ts` (E1-01) |
| 10.09.2026 | E0-08, ревью круг 1 | `packages/db/test/app-setting.int.test.ts:165,169` | новые строки пишут литерал `"queues.enabled"`, хотя `QUEUE_SWITCHES_KEY` введён тем же диффом и `seed.ts` уже переведён на константу | переименование ключа в core пройдёт seed и уронит тест сообщением о «пропавшей настройке» - прочитают как регресс seed | открыто: следующая задача по `@sf/db` |
| 10.09.2026 | E0-08, ревью круг 1 | `apps/worker/src/lib/queues.ts:50-52` (`opened()`), `apps/worker/src/jobs/index.ts:27` (реэкспорт `systemHeartbeatJob`/`systemSmokeJob`), `apps/worker/src/jobs/system-smoke.ts:24` (`SmokePayload`) | публичные символы без потребителя: метод фабрики `opened()`, реэкспорт джобов из барреля (два пути к одному символу), экспортированный тип payload'а | мёртвый код в публичном контракте пакета; следующий эпик закрепит один из двух путей импорта | открыто: E0-12 (чистка при выносе `lib/` в общий пакет) |
| 10.09.2026 | E0-08, ревью круг 1 | `packages/db/src/client.ts:5-12` | докблок про «connection is opened by an explicit call… ADR-0002» оторван от `createDb` новым докблоком `CONNECT_TIMEOUT_SEC` | IDE покажет к функции только последний блок; объяснение читатель не увидит | открыто: следующая задача по `@sf/db` |
| 10.09.2026 | E0-08, ревью круг 1 | `docs/30_E0_TASKS.md:40` (решение 5) против `packages/core/src/domain/job-ids.ts:97-102` | решение 5 требует кодировать часть, содержащую `/` или `:`; `embedJobId` кодирует только `:`, `/` вкладывает как есть - отступление записано строкой в `DECISIONS.md`, текст решения 5 не поправлен | E0-13 оформит решение 5 в ADR по исходному тексту и получит противоречие с билдером | открыто: E0-13 (ADR по билдерам `jobId`) |
| 10.09.2026 | E0-08, ревью круг 1 | `apps/worker/src/runtime.ts` (состав воркеров) против цитаты «Сделать» E0-08 «`Worker` на каждую очередь» | воркер поднимается только для очередей с процессором (2 из 29); подмена разумная (иначе джоб будущего эпика молча съедается), но зафиксирована в «Допущениях» плана, без строки в `DECISIONS.md` | следующий эпик ждёт воркер на своей очереди «из коробки», а его нет, пока имя не добавлено в `JOB_PROCESSORS` | открыто: строка техлида в `DECISIONS.md` (вынесено в «Открытые вопросы» файла дела E0-08) |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/src/jobs/system-smoke.ts:43` | вставка в `api_usage_log` не идемпотентна: повтор после сбоя на `moveToCompleted` пишет вторую строку с тем же `job_id` (уникального ключа у колонки нет) | при `units: 0` безвредно, но E0-09 копирует этот путь для платных вызовов - там это двойной учёт денег | открыто: E0-09 |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/src/lib/job-policy.ts:40` | `PAID_FANOUT_CAP = 2` захардкожен; тюнинг оператора обещан через `app_setting` | потолок платных очередей нельзя поднять без деплоя | открыто: E1-10 |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/package.json:16-18` | `@sf/youtube`, `@sf/gemini`, `@sf/tts` в `dependencies` без единого импортёра (наследие скелета) | лишние рёбра графа зависимостей и лишние пересборки turbo | открыто: убрать в эпике, который заведёт первый импорт (E1-01) |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/test/helpers.int.ts:25,63` | `openTestDb` дословно повторяет `packages/db/test/helpers.int.ts:47`; «все очереди включены» вычисляется второй раз, независимо от `defaultQueueSwitches` в `packages/db/src/seed.ts:12` (функция не экспортирована) | третья копия int-хелперов; дефолт seed перестал быть «всё true» - int-тесты воркера остаются зелёными по неверной причине | открыто: E0-12 (общий пресет и экспорт дефолтов seed) |
| 10.09.2026 | E0-08, исполнитель | `apps/worker/src/lib/` против `apps/api/src/lib/` | инфраструктура вперемешку с логикой очередей, две копии `redis`/`shutdown`/`logger` | третий потребитель разъедется с обоими; при выносе брать worker-вариант `shutdown` (`stages[]`) | открыто: E0-12 |
| 10.09.2026 | E0-08, исполнитель | `packages/db/src/seed.ts` (`pruneQueueSwitches`) | падает сырым `PostgresError`, если в `queues.enabled` лежит не объект | оператор с испорченной строкой получает `db:seed` с ошибкой драйвера вместо подсказки | открыто: следующая задача по seed |
| 10.09.2026 | E0-08, исполнитель | `api_usage_log.job_id` (миграция 0000) | индекса нет | разбор «что стоила эта джоба» пойдёт seq scan; на объёме MVP терпимо | принято: цена - seq scan по таблице расходов; переоткрыть, когда `api_usage_log` перевалит за ~1 млн строк или E13 заведёт разбор по `job_id` |
| 10.09.2026 | E0-08, ревью круг 2 (решение техлида 10.09) | `apps/api/package.json` (`dev: tsx watch src/server.ts`) | воркер ушёл на `node --import tsx src/index.ts` ради сигнала, а api остался под `tsx watch`: тот же `child.kill(signal)` и SIGKILL через 5 с при собственном дедлайне 10 с, на Windows - сразу `TerminateProcess` | Ctrl+C в dev рвёт запросы в полёте вместо graceful-закрытия Fastify; в логе ни строки о закрытии | открыто: E0-11 - оценить вместе с dev-профилем Compose (у api цена watch ниже: запрос короче джобы и не платный) |
| 10.09.2026 | E0-08, ревью круг 2 | `packages/db/src/repos/app-setting.ts:37-50` против `docs/adr/0003-queue-switches.md` | у одной JSONB-строки трое писателей (seed, E13-02, E1-08), а примитив записи один - `set`, заменяющий значение целиком: ни частичного обновления (`jsonb_set`/`\|\|`), ни условного `update … where value = expected`; каждый писатель делает read-modify-write | две команды паузы из UI подряд: вторая, посчитанная от значения до первой, возвращает `true` первой очереди - воркер резюмит её в течение минуты, хотя оператор видит «paused» | открыто: E13-02 (первый настоящий второй писатель) |
| 10.09.2026 | E0-08, ревью круг 2 | `apps/api/src/lib/logger.ts:19-21` и `apps/worker/src/lib/logger.ts:16-18` (`REDACTED_LOG_PATHS`) | список путей `redact` продублирован в двух приложениях | новый чувствительный путь допишут в одном месте; вторая копия продолжит писать значение в лог | открыто: E0-12 (вынос `lib/` в общий пакет, вместе со строкой про `redis`/`shutdown`/`logger`) |
| 10.09.2026 | E0-08, ревью круг 2 | `apps/worker/src/bootstrap.ts:37-43` | второй SIGINT только логируется («shutdown already in progress»): форсировать выход раньше дедлайна 25 с оператор не может | зависшая на закрытии стадия держит консоль оператора 25 с, и единственный выход - `taskkill`, то есть потеря активной задачи | открыто: E0-11 (там же `stop_grace_period` и dev-профиль) |
| 10.09.2026 | E0-08, ревью круг 2 | `apps/worker/src/lib/shutdown.ts:77` (`process.exit`) | при dev-транспорте `pino-pretty` строки уходят в отдельный поток; `process.exit` может срезать последние из них | в dev «shutdown complete» иногда не видно, и стоп выглядит как обрыв | открыто: вместе с выносом логгера (E0-12) |
| 10.09.2026 | E0-08, ревью круг 2 | `apps/worker/test/dlq.int.test.ts:45,125` | `jobIdFrom` в фикстурах собирает id шаблонной строкой мимо билдеров `@sf/core` (решение 5), поэтому int-путь `finalize()` не проверяет | запрет id из одних цифр и кодирование `:` держатся только юнит-тестами; регресс билдера в настоящем BullMQ не будет виден | открыто: следующая задача по воркеру |
| 10.09.2026 | E0-08, ревью круг 2 | `packages/db/src/seed.ts:56` | слияние `DEFAULT_APP_SETTINGS` и `pruneQueueSwitches` - два стейтмента без транзакции | обрыв между ними оставляет строку переключателей с ключами мёртвых очередей до следующего `db:seed` | принято: цена - один лишний `db:seed`; переоткрыть, когда seed начнёт писать связанные строки (первая - `queues.autopaused` в E1-08) |
| 10.09.2026 | E0-08, ревью круг 2 | `apps/api/src/server.ts:27-31` | до сборки `app` ошибки Redis уходят в noop-колбэк (наследие E0-06) | отказ Redis на старте api не виден нигде: ни строки в логе, ни метки в `/health` до первого запроса | открыто: E0-11 (healthcheck-и и старт в Compose) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/lib/logger.ts:16-18`, `apps/api/src/lib/logger.ts:19-21` (`REDACTED_LOG_PATHS`) | `redact` не покрывает вложенную ошибку `err.cause.command.args`: `*.command.args` - однобуквенный wildcard fast-redact, а не рекурсивный, и не проходит через второй уровень вложенности | ошибка ioredis, завёрнутая в `cause` другой ошибкой (`throw new Error(msg, { cause })`), пронесёт пароль Redis в лог тем же путём, который блокер круга 1 закрывал | открыто: вместе с выносом `lib/logger.ts` в общий пакет (E0-12) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/test/local-stack-guard.ts` (`TEST_DATABASE`) против `packages/db/test/helpers.int.ts:31-35` (`current_database() = 'shorts_factory_test'`) | литерал имени тестовой базы задублирован в двух пакетах двумя разными механизмами (сравнение pathname URL против SQL-запроса), и ни один не проверяет порт | переименование тестовой базы поправят в одном месте и забудут во втором; порт не проверяется - туннель на `localhost:5442` к чужой базе с тем же именем страж пропустит (тот же изъян, что и `TECH_DEBT:75`) | открыто: E0-12 (общий страж имени и порта) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/cli/smoke-run.ts` (`settleWithin`) против `apps/worker/src/lib/shutdown.ts:117` | «промис с дедлайном» - общий примитив, но лежит в файле одной CLI-команды; `shutdown.ts` пишет ту же гонку (`setTimeout` + отдельный резолвер) руками, второй копией | правка одной копии (например, гарантированная очистка таймера) не долетит до другой | открыто: E0-12 (вынос общего примитива в `lib/`) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/cli/smoke-run.ts` (`SMOKE_POLL_INTERVAL_MS`, `SmokeQueueState`, `probeQueueState`, `smokeTimeoutReason`) | экспортированы из модуля, но потребителя за пределами файла (и его теста) нет | мёртвый код в публичном контракте модуля; следующий читатель не понимает, какие символы можно менять свободно | открыто: E0-12 (чистка публичных экспортов `cli/`) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/test/helpers.int.ts:39` (`testLogger`) против `apps/worker/test/recording-logger.ts:43` (`silentLogger`) | два хелпера «тихий логгер» под разными именами в одном пакете | новый тест выбирает один из двух наугад; поведение (silent pino vs recording pino) расходится незаметно | открыто: E0-12 (один хелпер тестового логгера) |
| 10.09.2026 | E0-08, ревью круг 3 | `packages/db/src/client.ts` (`opts.connectTimeoutSec`) | параметр объявлен, но передаёт его только собственный тест (`client.test.ts`); ни api, ни worker своё значение не задают | опция выглядит настраиваемой конфигурацией, а на деле у неё один вызывающий | принято: цена - недоиспользованный параметр; переоткрыть, когда появится второй вызывающий с другим значением |
| 10.09.2026 | E0-08, ревью круг 3 | `packages/db/src/seed.ts:12` (`defaultQueueSwitches`) | строит `Record<string, boolean>` руками, не через `QueueSwitchesSchema.parse`, хотя воркер отказывает старт, если значение той же строки не проходит эту схему | рефакторинг реестра (например, смена формы имени очереди) разойдётся со схемой в `@sf/core`, seed останется зелёным, а воркер откажет со старта только на выкатке | открыто: следующая задача по `@sf/db` (провалидировать перед `insert`) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/cli/smoke-run.ts` (`runSmoke`, опрос состояния) | опрос джобы различает только `completed`/`failed`; `delayed` (провалившийся джоб, ждущий ретрая с backoff) не распознаётся отдельно | при лежащем Postgres (smoke-job валится на записи в `api_usage_log`, ждёт ретрая) `pnpm smoke` печатает «is the worker running?», хотя воркер жив и джоб просто ждёт следующей попытки | открыто: следующая задача по CLI (`smoke-run.ts`) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/lib/queue-switches.ts` (`applyQueueSwitches` → `queue.isPaused()`), `apps/worker/src/bootstrap.ts:99` | `isPaused()` идёт через `waitUntilReady`, которая с мёртвым Redis (`maxRetriesPerRequest: null`, offline-очередь) не резолвится никогда: старт воркера зависает, а не падает, и строка «worker failed to start» не печатается | оператор с лежащим Redis получает недиагностируемое зависание процесса вместо кода выхода и сообщения об ошибке | открыто: следующая задача по `bootstrap.ts`/`queue-switches.ts` (дедлайн на старт) |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/package.json` (`scripts.smoke`) против `apps/worker/test/entry-point.int.test.ts:141` (канарейка для `scripts.dev`) | у `dev` есть тест, что строка `package.json` равна ожидаемой команде; у `smoke` такого теста нет | правка `scripts.smoke` (случайный watcher, смена входного файла) пройдёт мимо всех тестов | открыто: следующая задача по CLI |
| 10.09.2026 | E0-08, ревью круг 3 | `apps/worker/src/cli/smoke.ts:57-73` | ошибка закрытия соединений (`events.close()`/`queues.close()`/`closeRedis`) поймана `.catch(() => false)` и превращена в общий таймаут-вердикт; сам `err` никуда не логируется | отказ закрытия (например, повторный `quit` на уже закрытом клиенте) неотличим в логе от честного таймаута - причина теряется | открыто: следующая задача по CLI |
