# Журнал решений

Решения, принятые PM и техлидом вместо владельца. Строка пишется **сразу после ответа**
агента, а не в конце захода. Это единственный документ, который владельцу стоит открыть,
чтобы увидеть, что нарешали без него. Решение можно пересмотреть - новой строкой со ссылкой
на прежнюю, а не правкой старой.

| Дата | Задача | Кто | Вопрос | Что выбрали | Почему |
| --- | --- | --- | --- | --- | --- |
| 05.09.2026 | обвязка | владелец | где живёт обвязка | внутри репозитория, `.claude/**` коммитится; состояние и файлы дела в `.gitignore` | один git, переносимость; портфолио показывает инженерную дисциплину |
| 05.09.2026 | обвязка | владелец | кто заменяет человека на развилках | два агента - PM (продукт, по документам) и техлид (техника с учётом продукта) | требование владельца: продуктовые решения без техники, технические - с продуктом |
| 05.09.2026 | обвязка | владелец | режим работы | одна задача на сессию, автономно до коммита, пауза между задачами | владелец хочет поглядывать между задачами и запускать следующую сам |
| 05.09.2026 | обвязка | владелец | первый коммит скелета без миграции и с обвязкой | разовые строки в unlock.txt (`.claude/hooks/**`, `settings.json`, `ci.yml`, `X01`) до первого коммита | базовой точки нет, весь скелет для замков «новый»; миграцию 0000 пишет E0-04 |
| 05.09.2026 | обвязка | владелец | как сделать первый коммит при замке, требующем /qa и гейты | одноразовая ветка bootstrap в commit-gate.js: без HEAD пропускаются только проверки /qa и гейтов; флаги, формат, мусор, ослабления действуют; проба в harness-selftest | базовой точки нет - отпечаток диффа не определён; условие после первого коммита не повторится |
| 05.09.2026 | обвязка | владелец | установщик падал на кириллице без BOM | UTF-8 BOM в `install.ps1`/`otkat.ps1`, проба на BOM в `harness-selftest.js`; строка unlock до коммита владельца | Windows PowerShell 5.1 читает .ps1 без BOM в системной кодировке |
| 06.09.2026 | E0-01 | техлид | build в apps/api и apps/worker при source-only пакетах | скрипт build убран из api и worker, `pnpm build` собирает только apps/web; `outDir` из их tsconfig удалён | решение 1 E0 (tsx без компиляции); tsc без rootDir тянул исходники пакетов в dist; откат - две строки |
| 06.09.2026 | E0-01 | техлид | `biome check .` задевает `.claude/**` и негитовые файлы | `vcs.useIgnoreFile` + `files.ignore` для `.claude/**` и `migrations/**`; команда lint не сужается | хуки написаны в другом стиле, переформат 16 защищённых файлов не цена E0-01; обвязку проверяет harness-selftest; корень и infra остаются под линтом |
| 06.09.2026 | E0-01 | техлид | куда ставить `@types/node` | в devDependencies пакетов, которые используют Node API (config, db, api, worker, web), не в корень | корневой пакет виден tsc через родительский node_modules и протекает в `@sf/core`; явная зависимость держит границу §6 |
| 06.09.2026 | E0-01 | техлид | правка защищённого `ci.yml` | убран `with.version` у `pnpm/action-setup@v4`, версию задаёт `packageManager` | два красных прогона «Multiple versions of pnpm specified»; шаги конвейера не снимаются |
| 06.09.2026 | E0-01 | техлид | заводить ли `globals.css` с директивами Tailwind и импорт в `layout.tsx` в E0-01 | да: минимальный файл из трёх директив + импорт | без входного css `next build` не прогоняет PostCSS/Tailwind, добавленные конфиги не проверяются гейтом `build`; темы и shadcn остаются в E0-10 |
| 06.09.2026 | E0-01 | техлид | включение гейта `build` в `rules.sf.json` | `when: never` -> glob по apps/packages/корневым конфигам, две пробы в `harness-selftest` | предусмотрено DoD E0-01 и текстом самого правила; усиление проверки |
| 06.09.2026 | E0-01 | техлид | `next build` с `output: "standalone"` красный на Windows без права на symlink; включать ли гейт `build` | `output` включается переключателем `NEXT_OUTPUT_STANDALONE=1` (по умолчанию выключен), его выставляет `web.Dockerfile` в E0-11; гейт `build` включён в E0-01 | локальный `pnpm build` и гейт честно зелёные, компиляция и бандл проверяются везде, упаковку standalone проверяет сборка образа; Developer Mode - действие владельца, снятие standalone - та же стена в E0-11; прецедент env в tooling-конфиге - `drizzle.config.ts:7` |
| 06.09.2026 | E0-01 | техлид | X01: шесть файлов схемы Drizzle в диффе без миграции | снято строкой `X01` в unlock.txt на время задачи | дифф - только формат Biome, содержимое совпадает с HEAD без пробелов и запятых; миграция 0000 - E0-04 |
| 06.09.2026 | E0-01 | техлид | X02: изменён `@sf/config` без правки `.env.example` | снято строкой `X02` в unlock.txt | правка - формат Biome одной цепочки, переменных не добавлено |
| 06.09.2026 | E0-01 | техлид | новый `pnpm-lock.yaml` под защитой | разовая строка `sf/pnpm-lock.yaml` в unlock.txt; файл коммитится | lockfile создан pnpm, DoD и D1 требуют его в репозитории; защита - от ручных правок, не от появления |
| 06.09.2026 | E0-01 | техлид | фактическое состояние гейта build | включение отложено - правка `.claude/hooks/**` отклонена системой разрешений сессии; строка выше о включении гейта описывает намерение, а не факт | гейт остаётся `when: never`; включение переносится отдельным заходом с разрешения владельца |
| 06.09.2026 | E0-01 | техлид | dev-зависимости web под Tailwind/PostCSS (`postcss`, `autoprefixer`, `@types/react-dom`) | добавить в `devDependencies` `apps/web` | названы в DoD задачи, dev-only, без внешних вызовов |
| 06.09.2026 | E0-01 | техлид | `tasks.mjs` пишет `tasks.json` не в форме Biome - пинг-понг гейтов `lint` и `tracker` | `docs/tasks/**` в `files.ignore` `biome.json`; файл в коммит идёт в форме `tasks.mjs render` | генерируемый файл, как `migrations/**`; учить трекер переносам Biome - дубль чужого форматтера; откат - одна строка |
| 06.09.2026 | E0-01 | техлид | `* text=auto eol=lf` обещает LF для `.ps1`, которые в индексе CRLF с BOM по решению владельца 05.09 | `*.ps1 -text` в `.gitattributes`, секция `[*.ps1]` `utf-8-bom`/`crlf` в `.editorconfig` | байты установщика не меняются, ренормализация не нужна; `text eol=crlf` дал бы дифф по защищённым файлам |
| 06.09.2026 | E0-01 | техлид | DoD «гейт build включён» невыполним: правка `.claude/hooks/**` отклонена системой разрешений сессии | E0-01 закрыть, гейт вынести в `E0-01A` (deps E0-01, перед E0-02), `30_E0_TASKS.md` со ссылкой | 5/6 DoD подтверждены прогоном; блокировка остановила бы всю цепочку; предусловие снимает только владелец, в отчёте это названо явно |
| 06.09.2026 | E0-02 | техлид | `passWithNoTests: true` из текста эпика против W03 и hard rule | не ставить; в каждом пакете со скриптом `test` есть содержательная канарейка; пункт эпика отменён припиской | замок W03 отобьёт коммит (`vitest.config.*` не в exempt), расширение exempt - дыра на все будущие конфиги; D7 закрывается тестами честно |
| 06.09.2026 | E0-02 | техлид | поведение `pnpm test:int` без Compose и в какую базу ходить | `turbo run test:int` без пропуска (без Compose - красный); отдельная база `shorts_factory_test` через init-скрипт Compose `infra/postgres/init/01-test-db.sql`; пункт эпика «пропускается без DATABASE_URL» отменён | скрипт с выходом нулём - гейт, который зелен без прогона (L-005, 60_HARNESS.md:184); общая база - риск чистки локальных данных int-тестами с E0-04; init-скрипт - ноль своего кода и зависимостей |
| 06.09.2026 | E0-02 | техлид | форма проб L-008 для гейтов `test`/`test:int` | `gates.js`: низ под `require.main === module`, экспорт `selectGates`; пробы зовут настоящий отбор по `rules.sf.json` (пропуск и отказ) | проба по копии разбора `when` не ловит регресс самого замка; правка закрыта существующей пробой `--list` и тем, что `require` не запускает `main`; та же форма закрывает E0-01A |
| 06.09.2026 | E0-02 | техлид | `vitest` в devDependencies `@sf/db` и правка защищённого lockfile | добавить `vitest ^2.1.0` в `packages/db`; разовая строка `sf/pnpm-lock.yaml` в unlock.txt | тот же пакет и диапазон, что в core/api, новых узлов в lock нет; int-тест соединения живёт в пакете-владельце `postgres`, а не в `apps/api` через транзитивную зависимость |
| 06.09.2026 | E0-02 | техлид | DoD «гейты test/test:int включены» невыполним: запись в `.claude/**` отклонена слоем разрешений сессии; lockfile без строки unlock | E0-02 закрыть шагами 1-6, гейты и пробы L-008 вынести в `E0-02A` (deps E0-02, blocked с причиной E0-01A, одним заходом с ней); lockfile коммитится без unlock, замечание Stop-хука - ожидаемое | `protect-files` не ловит Bash (`settings.json` matcher), `commit-gate` читает `forbiddenPaths`, а не `protected` - lockfile проходит; повторная блокировка остановила бы E0-03/E0-05 без пользы; предусловие уже у владельца в E0-01A |
| 06.09.2026 | обвязка | владелец | каждый запуск `/auto` вставал на замках записи файлов (`protect-files`/`unlock.txt`, `scope`/`scope.txt`, `diff-boundaries` X01/X02) раньше плана | замки сняты целиком: хуки удалены вместе с `qa-lock` (счётчик правок без /qa), `unlock.txt` и `scope.txt` больше нет, агент правит любые файлы в любом количестве | ошибки ловят семь ревьюеров, техлид и замок коммита; режим разрешений `bypassPermissions` |
| 06.09.2026 | E0-02 | техлид | int-тест читал `process.env.DATABASE_URL ?? ""` в обход `@sf/config` | `env` из `@sf/config` + ассерт `current_database() = 'shorts_factory_test'` литералом | Zod fail-fast вместо молчаливого коннекта на дефолты драйвера; Zod не видит имя базы - изоляцию решения B держит только явный ассерт; `@sf/db` не импортируется (D3) |
| 06.09.2026 | E0-02 | техлид | `process.loadEnvFile` не перекрывает переменные оболочки - `.env.test` молча проигрывает экспортированному `DATABASE_URL` | `readFileSync` + `util.parseEnv` + `Object.assign(process.env, …)` в `setup.ts`; `dotenv/config` в `src/index.ts` не трогается, единый загрузчик - E0-03 (TECH_DEBT) | встроенные модули: ENOENT бросается сам, перезапись явная; `dotenv.config` при отсутствии файла не бросает; два загрузчика - два контракта (runtime необязателен, фикстура обязательна), сводить их - в задаче, переписывающей парсинг env |
| 06.09.2026 | E0-02 | техлид | `.env.test` и `setup.ts` не в хеше turbo: правка фикстуры отвечает FULL TURBO, канарейка не выполняется (L-005) | `globalDependencies: [".env.test", "packages/config/vitest/setup.ts"]` в turbo.json | корневой файл без пакета-владельца и файл пакета без задачи `test` попадают в хеш только так; проба: правка .env.test обязана перезапустить `pnpm test` и покрасить канарейку |
| 06.09.2026 | E0-02 | техлид | core тянул `setup.ts` из `packages/config`; пакеты связаны строкой файлового пути; заводить ли пресет | `setupFiles` из core убран; `exports` в `@sf/config` с `./vitest/setup`, подключение `@sf/config/vitest/setup` в api/worker/db-int; пресет отложен до E0-12 (TECH_DEBT) | §6/S07: core без зависимостей; путь `../../packages/...` - S08 в обход регулярки, exports видим pnpm/tsc/vitest; пресет = `vitest` в devDeps config ради трёх одинаковых объектов - рано |
| 06.09.2026 | E0-02 | техлид | правило «`.env*` агент не пишет» против DoD «`.env.test` коммитится» | `.env.test` и `.env.example` - вне темы владельца 1: фикстура и шаблон без секретов, агент читает и правит; приписка в CLAUDE.md тема 1 и в `note` rules.sf.json | спека E0-02 объявляет файл несекретным, владелец сузил deny в settings.json и создал файл сам - правило догоняет факт; секреты остаются темой владельца, security проверяет каждый дифф файла |
| 06.09.2026 | E0-02 | техлид | Redis в `.env.test` без номера базы - тот же `db 0`, что у разработки | `REDIS_URL=redis://localhost:6379/1`, литерал канарейки обновлён | с E0-08 тестовые джобы легли бы в dev-ключи `bull:*`; та же изоляция, что `shorts_factory_test` (решение B) |
| 06.09.2026 | E0-02 | техлид | E0-02A и E0-01A стоят `blocked` с причиной, снятой владельцем 06.09; `next` берёт только `todo` | `tasks.mjs unblock` обеим; E0-02 закрывается `start` -> коммит правок -> `done` в этой сессии | иначе гейты `test`/`test:int`/`build` остаются `never` бессрочно; причина блокировки не редактируется, только снимается |
| 06.09.2026 | E0-02 | техлид | E0-01A/E0-02A разблокированы решением от 06.09, но запись в `rules.sf.json` в этом же заходе отклонена 5 раз | обе обратно в `blocked` через `tasks.mjs block` с точной причиной (файл, число отказов, источник - политика глобального `~/.claude/settings.json` для `.claude/hooks/**`); E0-02 закрывается без них | посылка «блок снят владельцем» была неверной; `todo` ведёт следующий заход в тот же отказ; `block` - единственный канал, который доносит причину до владельца через `00_STATUS.md` |
| 06.09.2026 | E0-02 | техлид | сабпат `@sf/config/vitest/setup` резолвится из рантайма и безусловно перетирает `process.env` | гейт в `setup.ts`: `process.env.VITEST !== "true"` -> throw; `exports` остаются | `VITEST` ставит сам раннер в воркере, `NODE_ENV=test` доказательством не является; молчаливый выход - L-004; откат `exports` возвращает S08-путь |
| 06.09.2026 | E0-02 | техлид | `Object.assign` фикстурой перекрывает только 5 ключей; `GEMINI_API_KEY` и прочие приезжают из оболочки вне turbo | `EnvSchema` в `src/schema.ts` без побочных эффектов; `loadTestEnv()` удаляет из `process.env` все ключи `EnvSchema.shape`, затем накладывает фикстуру; `setup.ts` - гейт + вызов | единственный источник списка ключей - схема; хардкод и заглушки в `.env.test` разъедутся со схемой, заглушка ключа заставит E1+ ходить в сеть; оставить - риск платного вызова с настоящего ключа |
| 06.09.2026 | E0-02 | техлид | `globalDependencies` не покрывает `packages/config/src/**` и `package.json`, а `.env.test` в нём сбрасывает `build`/`typecheck` | `@sf/config` получает задачу `test` (vitest, тесты загрузчика), `globalDependencies` снят, `test.inputs: ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.env.test"]`; пересмотр решения от 06.09 о `globalDependencies` | `^test` тянет хеш пакета целиком без ручного списка; turbo 2.10 поддерживает `$TURBO_ROOT$` - прежняя посылка «корневой файл только через globalDependencies» неверна; логика `setup.ts` проверяется в пакете-владельце |
| 06.09.2026 | E0-02 | техлид | зависимость: `vitest` в devDependencies `@sf/config` | добавить `vitest ^2.1.0`, lockfile пишет pnpm | тот же пакет и диапазон, что в core/api/db (решение от 06.09); новых узлов в lock нет; без него у пакета нет задачи `test` и его файлы не входят в хеш `^test` |
| 06.09.2026 | E0-02 | техлид | `CLAUDE.md:61-62` разрешает `.env.test`, `CLAUDE.md:118` и `note` rules.sf.json запрещают `.env*` целиком | обе строки CLAUDE.md сведены к одному правилу: исключение только для корневых `.env.example`/`.env.test`, условием «без настоящих значений, дифф проверяет security»; `note` в rules.sf.json - в «Сделать» E0-02A | сужение до корня закрывает `apps/*/.env.test`; условие вместо факта; `note` недостижим слоем разрешений и не является замком |
| 06.09.2026 | E0-03 | техлид | чем `env` ломает клиентскую сборку Next | зависимость `server-only` в `@sf/config`, импорт только в подвходе `@sf/config/server`; корневой вход без маркера; ADR 0001 | ошибку сборки Next даёт только точный спецификатор `server-only` (`next-invalid-import-error-loader`), свои условия `exports` дают лишь runtime-throw; в корне маркер уронил бы api/worker/db/vitest; S11 не видит подвход - строка TECH_DEBT на E0-01A/E0-10 |
| 06.09.2026 | E0-03 | техлид | чем читать файл env в едином загрузчике | `node:util.parseEnv` + `readFileSync` в `src/env-file.ts` для рантайма (override:false, путь от корня репозитория, пропуск под VITEST) и фикстуры (override:true); `dotenv` удалён из `@sf/config` | развязка, отложенная 06.09 до этой задачи; один разбор вместо двух контрактов; встроенный модуль Node 22 вместо зависимости; `dotenv` искал `.env` в cwd пакета и корневой файл не находил |
| 06.09.2026 | E0-03 | техлид | `ADMIN_PASSWORD` >= 16 в production: делать здесь или в E13-07 | здесь: `superRefine` по `NODE_ENV` на производной схеме в `load-env.ts`, `EnvSchema` остаётся `ZodObject` ради `.shape`; тест на три случая | `43_E13_TASKS.md:110` уже относит проверку к E0-03; `.env.example`/`.env.test` не production и остаются валидными; `refine` на `EnvSchema` убил бы `.shape`, на котором держится чистка env в фикстуре |
| 06.09.2026 | E0-03 | техлид | как доказать DoD «копия `.env.example` в `.env` запускает api/worker» без создания `.env` | автотест на временной копии `.env.example` вне репозитория через полную цепочку `readEnvFile` -> `applyEnvFile` -> `loadEnv` + тест резолва пути от корня + ручной прогон api/worker на переменных процесса без вывода значений | `.env` - тема владельца 1, у него там могут быть ключи; DoD проверяет парсинг, а не `cp`; пауза цикла ради того, что закрывает тест, не нужна |
| 06.09.2026 | E0-03 | PM | должен ли браузерный код знать значения лимитов из конфига или получать их только из API | B: лимиты только на сервере, веб берёт предел/расход/процент из `/system/status` (E0-09) | `/system` рендерится с сервера (E0-10, решение 2) - лишнего похода нет; действующий предел зависит от env, вшитая константа показала бы оператору неверное «Y»; сценария показа предела без API в документах нет |
| 06.09.2026 | E0-03 | техлид | корневой `@sf/config` в клиентском компоненте валит сборку только свойством резолва webpack (`Module not found: ./env-file.js`), не замком; DoD называет именно корневой вход | пересмотр строки Р1 от 06.09 в части `exports`: `"."` = `{ "browser": "./src/server.ts", "default": "./src/index.ts" }`; подвход `./server`, маркер и ADR-0001 остаются (ADR дополнен); формулировка DoD не меняется | в браузерной сборке корень резолвится в модуль с `server-only` - та же ошибка того же загрузчика Next, а не runtime-throw (этим отличается от отклонённого throw-модуля); Node/tsx/vitest/tsc (`NodeNext`) условие `browser` не берут; нынешняя защита исчезнет в E0-10, где резолв `.js`->`.ts` нужен самому `server.ts`; A закрепляет дыру, C дублирует S11 линтом без ошибки сборки |
| 06.09.2026 | E0-03 | техлид | сборочная проба корневого входа не доходит до клиентского компилятора: серверный падает первым на `Module not found: ./env-file.js` (нет резолва `.js`->`.ts` под `transpilePackages`), а `extensionAlias` ранее отправлена в TECH_DEBT на E0-10 | `experimental.extensionAlias: { ".js": [".ts", ".tsx", ".js"] }` в `apps/web/next.config.ts` в E0-03, проба повторяется до строки про `server-only`; строка TECH_DEBT закрывается досрочно | DoD «ошибка сборки» доказывается только сборкой (L-001/L-005), резолв и юнит-тест - доказательство другого утверждения; опция - предусловие уже сделанной проводки `transpilePackages`, без неё `server.ts -> ./index.js` не собирается и в E0-10; штатная опция Next, откат - три строки |
| 06.09.2026 | E0-03 | техлид | третий барьер корневой пробы: webpack читает `new URL("../../../", import.meta.url)` в `paths.ts:11` как ресурс, серверный компилятор падает, клиентский не стартует; довести, делить или менять план | довести: `repoRoot` = `resolve(dirname(fileURLToPath(import.meta.url)), "../../..")`, проба повторяется; строка TECH_DEBT про корень после бандла остаётся открытой (E0-10/E0-11); стоп-правило - при новом барьере DoD закрывается пробой подвхода, остаток строкой в E0-10, текст DoD не сужается | форма выражения без смены семантики, посадка прежними тестами; без неё проводка `transpilePackages` мёртвая при любом импорте из web - дефект этой задачи, не E0-10; граница «`paths.ts` не трогать» противоречила требованию к пробе - снята; план шага 8 не проверил достаточность `transpilePackages` |
| 06.09.2026 | E0-03 | техлид | копия `.env.example` (без `NODE_ENV` -> production) проходит правило `>= 16` только потому, что заглушка удлинена до 20 символов: публичный пароль стартует как production без ошибки; обоснование строки Р3 от 06.09 («пример не production») больше не описывает код | `ADMIN_PASSWORD=` в примере пустой, как остальные секреты; копия падает одной ошибкой `ADMIN_PASSWORD`; DoD «без ошибок парсинга» уточнён припиской в `30_E0_TASKS.md`: по незаполненным необязательным ключам (D2), обязательный секрет пустой и падает с именем поля; дефолт `production` остаётся; уточнение к строке Р3: пример валидируется как production и валиден только после того, как оператор задал пароль, `.env.test` - не production | работающий пароль в публичном шаблоне недопустим ни для какой среды; «короче 16» оставляет его рабочим в development; fail-fast по обязательному секрету - цель задачи; откат - строка примера и три теста |
| 06.09.2026 | E0-03 | PM | приписка к DoD «без ошибок парсинга = одна ошибка по обязательному секрету» - уточнение или смена смысла критерия | уточнение в рамках задачи, но приписка снята, DoD E0-03 и E0-11 переписаны явно: нетронутая копия шаблона падает одной ошибкой `ADMIN_PASSWORD`, копия с заданным паролем стартует без ошибок парсинга | AC E0 в `20_TZ` §E0 старт с шаблонным паролем не обещает; цель E0-03 - fail-fast, тест «отсутствующий `ADMIN_PASSWORD`» уже в «Сделать»; рабочий пароль в шаблоне нарушает условие темы владельца 1 (`CLAUDE.md`); DoD должен называть исход для оператора, а не переопределять слово «без ошибок» |
| 06.09.2026 | E13-07 | PM | предупреждение на `/system` «пароль из `.env.example`» осталось без предмета после того, как шаблон везёт пустой `ADMIN_PASSWORD` | заменить на предупреждение «действующий `ADMIN_PASSWORD` короче 16 символов»; сравнение с шаблоном из `43_E13_TASKS.md:110` убрать | цель пункта - не жить с известным/слабым паролем; известного больше нет, production с коротким не стартует (E0-03); остаётся только `development` с коротким паролем - предупреждение по длине закрывает его и молчит у остальных; значение пароля не показывать |
| 06.09.2026 | E0-03 | техлид | `env-example.test.ts:21` читает корневой `.env.example`, которого нет в `test.inputs`: возврат пароля-заглушки в шаблон отвечает FULL TURBO | `"$TURBO_ROOT$/.env.example"` в `test.inputs` рядом с `.env.test`; `pnpm-workspace.yaml` не добавлять; `typecheck`/`build` - прежние строки TECH_DEBT (E0-12/E0-10) | та же форма, что для `.env.test` 06.09; пример меняется только вместе со схемой, лишних miss нет; `globalDependencies` сбрасывает build/typecheck; проверка существования `pnpm-workspace.yaml` не может измениться без остановки самого turbo; посадка - два прогона без `--force` |
| 06.09.2026 | E0-03 | техлид | дефолт `production` живёт в `env`, `process.env.NODE_ENV` остаётся пустым: `/health` говорит production, сторонние библиотеки идут по dev-ветке | не чинить; TECH_DEBT «принято» с ценой и условием переоткрытия (зависимость api/worker с ветвлением по `NODE_ENV` - проверить в E0-06/E0-08); E0-06 - pretty по `env.NODE_ENV`, E0-11 - `NODE_ENV` в Compose | запись в `process.env.NODE_ENV` в `config/src/index.ts` ломает `next build` под `transpilePackages` (DefinePlugin инлайнит цель присваивания), web `NODE_ENV` получает от Next; свой код читает только `env.NODE_ENV` (S05); копии в apps - дубль и обход `@sf/config` |
| 06.09.2026 | E0-03 | техлид | верхняя граница `WORKER_CONCURRENCY` | `.max(20)`, тест на 21/20 | одна машина, дефолт 5; опечатка 50 - полсотни платных джоб до срабатывания дневного кап; портам граница поставлена в этом же диффе |
| 06.09.2026 | E0-03 | техлид | дефолт `NODE_ENV` после снятия `NODE_ENV=development` из `.env.example` (находка code-review: правило Р3 в production молча не срабатывало) - fail-safe `production` или прежний `development` | подтверждён `production` (`schema.ts:35`), `development`/`test` только явным объявлением; дефолт в `process.env` не пишется; Compose E0-11 объявляет `NODE_ENV` явно | незадекларированная машина получает строгие правила, а не послабления; «без дефолта» ломает DoD про копию примера, «>=16 всегда» меняет E13-07, «оставить development» возвращает находку; откат - одна строка схемы |
| 06.09.2026 | E0-03 | техлид | техническая раскладка `limits` при решении PM «только сервер» | `src/limits.ts` внутренний (`Limits`, `buildLimits(env)`), без `DEFAULT_LIMITS` и без подвхода `./limits`; `exports` только `.` и `./server`; `buildLimits` доступен из корня для тестов и E0-09 | у клиента нет потребителя - публичный подвход был бы мёртвой поверхностью; дефолты уже в `.default()` схемы, второй объект - дубль; корень в web режет замок S11, подвход - `server-only`; добавить `./limits` позже - одна строка, убрать использованный - дороже |
| 06.09.2026 | E0-03 | техлид | «задано» решается по `trim()`, а значение кладётся в `env` сырым: хвостовой пробел становится частью пароля, 15 пробелов и символ проходят правило `>= 16` | обрезать все значения в `loadEnv` (одно понятие: значение после `trim()`, пустое после `trim()` - не задано); `process.env` и загрузчики файла не трогать; README - краевые пробелы не значимы даже в кавычках; четыре теста | та же рассинхронизация двух половин, что в блокере круга 1, на уровне символов; краевой пробел не значим ни для одного ключа схемы; техдолг «принято» - отложенный дефект входа в E0-06; откат - одна функция |
| 06.09.2026 | E0-03 | техлид | пересмотр строки о `.max(20)` выше: обоснование считало `WORKER_CONCURRENCY` потолком процесса, а `30_E0_TASKS.md:194` применяет его к каждой очереди (14 сейчас, ~24 по §4) | `.max(10)`, тест 11/10, комментарий в `schema.ts` - «на очередь», дефолт 5 не меняется; TECH_DEBT на E0-08: платным fan-out очередям явный cap в реестре | денежный аргумент неверен при любом множителе - расход сторожит guard E0-09 и капы §8, а не параллелизм; `.max()` - ресурсный предохранитель одной машины и типовая опечатка `50`; 10x24 обработчиков - предел смысла для одного VPS, 20x24 - нет; число очередей в `@sf/config` не втащить (§6) |
