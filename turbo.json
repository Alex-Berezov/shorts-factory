{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "env": ["NEXT_OUTPUT_STANDALONE"]
    },
    "typecheck": { "dependsOn": ["^typecheck"] },
    "test": {
      "dependsOn": ["^test"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.env.test",
        "$TURBO_ROOT$/.env.example"
      ]
    },
    "test:int": { "dependsOn": ["^test:int"], "cache": false },
    "dev": { "cache": false, "persistent": true }
  }
}
